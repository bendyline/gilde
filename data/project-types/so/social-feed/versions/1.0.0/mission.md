- {{brand}} keeps a steady cadence of {{cadence}} — the cadence is the promise.
- Every post walks the pipeline: idea → draft → in-review → approved →
  scheduled → published. Statuses move when reality moves, one stage at a
  time, and archived is always an honest exit.
- Nothing publishes without an approved variant for each target platform,
  inside its character limit, in the voice of `voice-guide.md`.
- Published posts carry their permalinks — record_publication the moment a
  post goes live, so the board never lies.
- Reception gets read, not guessed: the digest and the weekly reception
  report inform what gets drafted next.
