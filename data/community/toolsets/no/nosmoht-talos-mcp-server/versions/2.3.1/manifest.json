{
  "schemaVersion": 1,
  "version": "2.3.1",
  "releasedAt": "2026-04-16T11:42:31.304773Z",
  "runtime": {
    "kind": "npm-package",
    "package": "talos-mcp",
    "version": "2.3.1",
    "sha256": "77affe8a49aad54973916f4114867452305d006c53982ed2bc22ebf7d084e5c0",
    "entry": "bin/run.js",
    "args": [],
    "envHints": [
      "TALOSCONFIG",
      "TALOS_CONTEXT",
      "TALOS_ENDPOINTS",
      "TALOS_MCP_READ_ONLY",
      "TALOS_MCP_HTTP_ADDR",
      "TALOS_MCP_AUTH_TOKEN",
      "TALOS_MCP_ALLOWED_NODES",
      "TALOS_MCP_ALLOWED_PATHS",
      "TALOS_MCP_SKIP_VERSION_CHECK",
      "TALOS_MCP_RATE_LIMIT",
      "TALOS_MCP_RATE_BURST",
      "TALOS_MCP_MAX_BODY_SIZE",
      "TALOS_MCP_MAX_CONCURRENT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "TALOSCONFIG",
      "envVar": "TALOSCONFIG",
      "label": "Path to talosconfig file (default: ~/",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to talosconfig file (default: ~/.talos/config)"
    },
    {
      "id": "TALOS_CONTEXT",
      "envVar": "TALOS_CONTEXT",
      "label": "Context name override (default: active context from config)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Context name override (default: active context from config)"
    },
    {
      "id": "TALOS_ENDPOINTS",
      "envVar": "TALOS_ENDPOINTS",
      "label": "Comma-separated endpoint overrides",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated endpoint overrides"
    },
    {
      "id": "TALOS_MCP_READ_ONLY",
      "envVar": "TALOS_MCP_READ_ONLY",
      "label": "Set to \"true\" to disable all mutating tools (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to disable all mutating tools (default: false)"
    },
    {
      "id": "TALOS_MCP_HTTP_ADDR",
      "envVar": "TALOS_MCP_HTTP_ADDR",
      "label": "If set (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "If set (e.g. \":8080\"), serve Streamable HTTP instead of stdio"
    },
    {
      "id": "TALOS_MCP_AUTH_TOKEN",
      "envVar": "TALOS_MCP_AUTH_TOKEN",
      "label": "Required bearer token when HTTP mode is active (TALOS_MCP_HTTP_ADDR is set)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Required bearer token when HTTP mode is active (TALOS_MCP_HTTP_ADDR is set)"
    },
    {
      "id": "TALOS_MCP_ALLOWED_NODES",
      "envVar": "TALOS_MCP_ALLOWED_NODES",
      "label": "Comma-separated IPs, hostnames, and CIDR ranges permitted as tool targets",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated IPs, hostnames, and CIDR ranges permitted as tool targets. Unset or empty allows all nodes."
    },
    {
      "id": "TALOS_MCP_ALLOWED_PATHS",
      "envVar": "TALOS_MCP_ALLOWED_PATHS",
      "label": "Comma-separated path prefixes permitted for talos_read_file and talos_list_files (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated path prefixes permitted for talos_read_file and talos_list_files (e.g. /etc,/proc). Unset or empty allows all paths."
    },
    {
      "id": "TALOS_MCP_SKIP_VERSION_CHECK",
      "envVar": "TALOS_MCP_SKIP_VERSION_CHECK",
      "label": "Set to \"true\" to bypass upgrade path validation (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to bypass upgrade path validation (default: false)"
    },
    {
      "id": "TALOS_MCP_RATE_LIMIT",
      "envVar": "TALOS_MCP_RATE_LIMIT",
      "label": "HTTP mode: token-bucket refill rate in requests/second (float, default: 10)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP mode: token-bucket refill rate in requests/second (float, default: 10)"
    },
    {
      "id": "TALOS_MCP_RATE_BURST",
      "envVar": "TALOS_MCP_RATE_BURST",
      "label": "HTTP mode: token-bucket burst capacity (int, default: 20)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP mode: token-bucket burst capacity (int, default: 20)"
    },
    {
      "id": "TALOS_MCP_MAX_BODY_SIZE",
      "envVar": "TALOS_MCP_MAX_BODY_SIZE",
      "label": "HTTP mode: max POST request body size in bytes (int, default: 4194304 = 4 MiB)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP mode: max POST request body size in bytes (int, default: 4194304 = 4 MiB)"
    },
    {
      "id": "TALOS_MCP_MAX_CONCURRENT",
      "envVar": "TALOS_MCP_MAX_CONCURRENT",
      "label": "HTTP mode: max concurrent POST handlers; returns 503 on overload (int, default: 20)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP mode: max concurrent POST handlers; returns 503 on overload (int, default: 20)"
    }
  ]
}
