{
  "schemaVersion": 1,
  "version": "0.3.2",
  "releasedAt": "2026-05-12T10:32:03.89265Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@noticed/cli",
    "version": "0.3.2",
    "sha256": "419868a46e792099d5be567855c90d2333a022608dc9cf010b77d31ae31f7d46",
    "entry": "bin/noticed.js",
    "args": [
      "mcp"
    ],
    "envHints": [
      "NOTICED_API_URL",
      "NOTICED_API_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "NOTICED_API_URL",
      "envVar": "NOTICED_API_URL",
      "label": "Base URL of the noticed instance",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the noticed instance. Defaults to https://www.noticed.so — only set this if you self-host noticed."
    },
    {
      "id": "NOTICED_API_KEY",
      "envVar": "NOTICED_API_KEY",
      "label": "API key minted at /dashboard/api-keys",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "API key minted at /dashboard/api-keys"
    }
  ]
}
