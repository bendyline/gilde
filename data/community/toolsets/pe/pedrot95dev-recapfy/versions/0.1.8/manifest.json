{
  "schemaVersion": 1,
  "version": "0.1.8",
  "releasedAt": "2026-06-29T21:16:21.702552Z",
  "runtime": {
    "kind": "npm-package",
    "package": "recapfy-mcp",
    "version": "0.1.8",
    "sha256": "bc35fc9d2138339a85a7b4e7c3c18ae732de314cb4da39e254689815e9646928",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "SVM_PRIVATE_KEY",
      "RECAPFY_API_BASE_URL",
      "RECAPFY_ALLOW_INSECURE_TLS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SVM_PRIVATE_KEY",
      "envVar": "SVM_PRIVATE_KEY",
      "label": "Base58-encoded Solana secret key",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Base58-encoded Solana secret key. Pays per call (dynamic USDC price). Keep it funded; use a dedicated low-balance wallet."
    },
    {
      "id": "RECAPFY_API_BASE_URL",
      "envVar": "RECAPFY_API_BASE_URL",
      "label": "Override the API base URL (defaults to https://api",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the API base URL (defaults to https://api.recapfy.ai). For local dev only."
    },
    {
      "id": "RECAPFY_ALLOW_INSECURE_TLS",
      "envVar": "RECAPFY_ALLOW_INSECURE_TLS",
      "label": "Set to 1 to accept self-signed TLS (localhost dev over https only)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 1 to accept self-signed TLS (localhost dev over https only)."
    }
  ]
}
