{
  "schemaVersion": 1,
  "version": "0.17.0",
  "releasedAt": "2026-08-13T17:06:35.405607Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@professional-wiki/mediawiki-mcp-server",
    "version": "0.17.0",
    "sha256": "29a1d2c02fe0a8b157b5514f3e3f4f02def3e864cd97fcb5b20aa60638c4d7fd",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "CONFIG",
      "MCP_ALLOW_STATIC_FALLBACK",
      "MCP_CONTENT_MAX_BYTES",
      "MCP_FILE_DATA_MAX_BYTES",
      "MCP_LOG_LEVEL",
      "MCP_MAX_REQUEST_BODY",
      "MCP_METRICS",
      "MCP_OAUTH_ALLOWED_REDIRECTS",
      "MCP_OAUTH_CIMD_ALLOWED_HOSTS",
      "MCP_OAUTH_CREDENTIALS_FILE",
      "MCP_OAUTH_NO_BROWSER",
      "MCP_OAUTH_PROXY_STORE_FILE",
      "MCP_PUBLIC_URL",
      "MCP_SHUTDOWN_GRACE_MS",
      "MCP_TRANSPORT",
      "MCP_UPLOAD_MAX_BYTES",
      "PORT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "CONFIG",
      "envVar": "CONFIG",
      "label": "Path to your configuration file",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to your configuration file",
      "default": "config.json"
    },
    {
      "id": "MCP_ALLOW_STATIC_FALLBACK",
      "envVar": "MCP_ALLOW_STATIC_FALLBACK",
      "label": "Set to 'true' to allow HTTP startup when config",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'true' to allow HTTP startup when config.json has static credentials. Otherwise the server refuses to start, preventing silent shared-identity fallback for unauthenticated requests."
    },
    {
      "id": "MCP_CONTENT_MAX_BYTES",
      "envVar": "MCP_CONTENT_MAX_BYTES",
      "label": "Byte cap for the content bodies and result blocks tools return (wikitext, rendered HTML, diffs, statement and row listings)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Byte cap for the content bodies and result blocks tools return (wikitext, rendered HTML, diffs, statement and row listings). An oversized response is truncated with a trailing marker describing what was left out.",
      "default": "50000"
    },
    {
      "id": "MCP_FILE_DATA_MAX_BYTES",
      "envVar": "MCP_FILE_DATA_MAX_BYTES",
      "label": "Hard cap on the base64-encoded size of a get-file-data response",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Hard cap on the base64-encoded size of a get-file-data response. A transport/safety backstop; callers tune actual size with the tool's width parameter. Over-cap responses error rather than truncate.",
      "default": "1000000"
    },
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Minimum severity for logger output (stderr telemetry and sendLoggingMessage broadcast)",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Minimum severity for logger output (stderr telemetry and sendLoggingMessage broadcast). Invalid values fail loudly on first log call.",
      "default": "debug",
      "options": [
        "debug",
        "info",
        "notice",
        "warning",
        "error",
        "critical",
        "alert",
        "emergency",
        "silent"
      ]
    },
    {
      "id": "MCP_MAX_REQUEST_BODY",
      "envVar": "MCP_MAX_REQUEST_BODY",
      "label": "Maximum HTTP request body size on the StreamableHTTP transport",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum HTTP request body size on the StreamableHTTP transport. Accepts size strings like 1mb or 512kb.",
      "default": "1mb"
    },
    {
      "id": "MCP_METRICS",
      "envVar": "MCP_METRICS",
      "label": "Set to 'true' to expose Prometheus metrics at GET /metrics on the HTTP transport",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'true' to expose Prometheus metrics at GET /metrics on the HTTP transport. Has no effect on the stdio transport."
    },
    {
      "id": "MCP_OAUTH_ALLOWED_REDIRECTS",
      "envVar": "MCP_OAUTH_ALLOWED_REDIRECTS",
      "label": "Additional allowed redirect URIs for hosted-proxy client registration: comma-separated exact URIs (which must include a host) and https prefix patterns ending in /*",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Additional allowed redirect URIs for hosted-proxy client registration: comma-separated exact URIs (which must include a host) and https prefix patterns ending in /*. Loopback, claude.ai, and verified first-party clients (ChatGPT, Cursor, VS Code) are always allowed; unset means no additional clients."
    },
    {
      "id": "MCP_OAUTH_CIMD_ALLOWED_HOSTS",
      "envVar": "MCP_OAUTH_CIMD_ALLOWED_HOSTS",
      "label": "Comma-separated additional client_id document hosts trusted for CIMD (bare host or host:port)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated additional client_id document hosts trusted for CIMD (bare host or host:port). Verified first-party hosts are always trusted."
    },
    {
      "id": "MCP_OAUTH_CREDENTIALS_FILE",
      "envVar": "MCP_OAUTH_CREDENTIALS_FILE",
      "label": "Override the default OAuth credentials store path",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the default OAuth credentials store path. Default: ~/.config/mediawiki-mcp/credentials.json on Linux/macOS, %APPDATA%\\mediawiki-mcp\\credentials.json on Windows."
    },
    {
      "id": "MCP_OAUTH_NO_BROWSER",
      "envVar": "MCP_OAUTH_NO_BROWSER",
      "label": "Set to '1' to skip launching a browser during the OAuth dance and log the auth URL to stderr instead",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to '1' to skip launching a browser during the OAuth dance and log the auth URL to stderr instead. Useful in headless environments and CI."
    },
    {
      "id": "MCP_OAUTH_PROXY_STORE_FILE",
      "envVar": "MCP_OAUTH_PROXY_STORE_FILE",
      "label": "Path to the hosted OAuth proxy's encrypted state file, which lets sign-ins survive restarts",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to the hosted OAuth proxy's encrypted state file, which lets sign-ins survive restarts. Default: proxy-store.enc under the mediawiki-mcp config dir; /app/data/proxy-store.enc in the Docker image."
    },
    {
      "id": "MCP_PUBLIC_URL",
      "envVar": "MCP_PUBLIC_URL",
      "label": "Override the request-derived public URL used in the protected-resource discovery doc and WWW-Authenticate header",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the request-derived public URL used in the protected-resource discovery doc and WWW-Authenticate header. Set this when running behind a proxy that rewrites the request Host."
    },
    {
      "id": "MCP_SHUTDOWN_GRACE_MS",
      "envVar": "MCP_SHUTDOWN_GRACE_MS",
      "label": "Maximum milliseconds to wait for in-flight /mcp calls to drain on SIGTERM/SIGINT before exiting",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum milliseconds to wait for in-flight /mcp calls to drain on SIGTERM/SIGINT before exiting. Capped at 600000.",
      "default": "10000"
    },
    {
      "id": "MCP_TRANSPORT",
      "envVar": "MCP_TRANSPORT",
      "label": "Type of MCP server transport",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Type of MCP server transport",
      "default": "stdio",
      "options": [
        "stdio",
        "http"
      ]
    },
    {
      "id": "MCP_UPLOAD_MAX_BYTES",
      "envVar": "MCP_UPLOAD_MAX_BYTES",
      "label": "Memory cap on the server-side fetch used by upload-file-from-url and update-file-from-url",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Memory cap on the server-side fetch used by upload-file-from-url and update-file-from-url. Files larger than this are routed to the wiki's own copy-upload instead of being buffered by the server. Guards this server's memory, not the wiki's $wgMaxUploadSize.",
      "default": "104857600"
    },
    {
      "id": "PORT",
      "envVar": "PORT",
      "label": "Port used for StreamableHTTP transport",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Port used for StreamableHTTP transport",
      "default": "3000"
    }
  ]
}
