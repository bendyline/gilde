{
  "schemaVersion": 1,
  "version": "0.9.0",
  "releasedAt": "2026-05-01T04:32:03.826162Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@professional-wiki/mediawiki-mcp-server",
    "version": "0.9.0",
    "sha256": "0e65d9e9cbc38d4196b864248cd4d1732094a32f3309f22b984c2028e9bc3261",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "CONFIG",
      "MCP_ALLOW_STATIC_FALLBACK",
      "MCP_CONTENT_MAX_BYTES",
      "MCP_LOG_LEVEL",
      "MCP_MAX_REQUEST_BODY",
      "MCP_METRICS",
      "MCP_OAUTH_CREDENTIALS_FILE",
      "MCP_OAUTH_NO_BROWSER",
      "MCP_PUBLIC_URL",
      "MCP_SHUTDOWN_GRACE_MS",
      "MCP_TRANSPORT",
      "PORT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "CONFIG",
      "envVar": "CONFIG",
      "label": "Path to your configuration file",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to your configuration file",
      "default": "config.json"
    },
    {
      "id": "MCP_ALLOW_STATIC_FALLBACK",
      "envVar": "MCP_ALLOW_STATIC_FALLBACK",
      "label": "Set to 'true' to allow HTTP startup when config",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'true' to allow HTTP startup when config.json has static credentials. Otherwise the server refuses to start, preventing silent shared-identity fallback for unauthenticated requests."
    },
    {
      "id": "MCP_CONTENT_MAX_BYTES",
      "envVar": "MCP_CONTENT_MAX_BYTES",
      "label": "Byte cap for content bodies (wikitext, rendered HTML, diffs) returned by get-page, get-pages, parse-wikitext, and compare-pages",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Byte cap for content bodies (wikitext, rendered HTML, diffs) returned by get-page, get-pages, parse-wikitext, and compare-pages. Oversized bodies are truncated with a trailing marker.",
      "default": "50000"
    },
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Minimum severity for logger output (stderr telemetry and sendLoggingMessage broadcast)",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Minimum severity for logger output (stderr telemetry and sendLoggingMessage broadcast). Invalid values fail loudly on first log call.",
      "default": "debug",
      "options": [
        "debug",
        "info",
        "notice",
        "warning",
        "error",
        "critical",
        "alert",
        "emergency",
        "silent"
      ]
    },
    {
      "id": "MCP_MAX_REQUEST_BODY",
      "envVar": "MCP_MAX_REQUEST_BODY",
      "label": "Maximum HTTP request body size on the StreamableHTTP transport",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum HTTP request body size on the StreamableHTTP transport. Accepts size strings like 1mb or 512kb.",
      "default": "1mb"
    },
    {
      "id": "MCP_METRICS",
      "envVar": "MCP_METRICS",
      "label": "Set to 'true' to expose Prometheus metrics at GET /metrics on the HTTP transport",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'true' to expose Prometheus metrics at GET /metrics on the HTTP transport. Has no effect on the stdio transport."
    },
    {
      "id": "MCP_OAUTH_CREDENTIALS_FILE",
      "envVar": "MCP_OAUTH_CREDENTIALS_FILE",
      "label": "Override the default OAuth credentials store path",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the default OAuth credentials store path. Default: ~/.config/mediawiki-mcp/credentials.json on Linux/macOS, %APPDATA%\\mediawiki-mcp\\credentials.json on Windows."
    },
    {
      "id": "MCP_OAUTH_NO_BROWSER",
      "envVar": "MCP_OAUTH_NO_BROWSER",
      "label": "Set to '1' to skip launching a browser during the OAuth dance and log the auth URL to stderr instead",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to '1' to skip launching a browser during the OAuth dance and log the auth URL to stderr instead. Useful in headless environments and CI."
    },
    {
      "id": "MCP_PUBLIC_URL",
      "envVar": "MCP_PUBLIC_URL",
      "label": "Override the request-derived public URL used in the protected-resource discovery doc and WWW-Authenticate header",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the request-derived public URL used in the protected-resource discovery doc and WWW-Authenticate header. Set this when running behind a proxy that rewrites the request Host."
    },
    {
      "id": "MCP_SHUTDOWN_GRACE_MS",
      "envVar": "MCP_SHUTDOWN_GRACE_MS",
      "label": "Maximum milliseconds to wait for in-flight /mcp calls to drain on SIGTERM/SIGINT before exiting",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum milliseconds to wait for in-flight /mcp calls to drain on SIGTERM/SIGINT before exiting. Capped at 600000.",
      "default": "10000"
    },
    {
      "id": "MCP_TRANSPORT",
      "envVar": "MCP_TRANSPORT",
      "label": "Type of MCP server transport",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Type of MCP server transport",
      "default": "stdio",
      "options": [
        "stdio",
        "http"
      ]
    },
    {
      "id": "PORT",
      "envVar": "PORT",
      "label": "Port used for StreamableHTTP transport",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Port used for StreamableHTTP transport",
      "default": "3000"
    }
  ]
}
