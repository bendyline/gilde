{
  "schemaVersion": 1,
  "version": "0.1.10",
  "releasedAt": "2026-09-04T23:18:25.068686Z",
  "runtime": {
    "kind": "npm-package",
    "package": "postgres-mcp-hardened",
    "version": "0.1.10",
    "sha256": "020049ca51a2eb6b46f6d6f892ad80058d2bb872521718691db83489085ac97a",
    "entry": "bin/cli.js",
    "args": [],
    "envHints": [
      "DATABASE_URL",
      "MCP_STATEMENT_TIMEOUT",
      "MCP_ALLOW_TABLES",
      "MCP_AUDIT_LOG"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "DATABASE_URL",
      "envVar": "DATABASE_URL",
      "label": "Connection string for the role the server connects as",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Connection string for the role the server connects as. Use a role that cannot write — the server refuses writes twice, but a read-only role is the layer that does not depend on us being correct. `--print-setup-sql` prints the SQL that creates one."
    },
    {
      "id": "MCP_STATEMENT_TIMEOUT",
      "envVar": "MCP_STATEMENT_TIMEOUT",
      "label": "Server-side statement timeout, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Server-side statement timeout, e.g. `5s`. A question that would pin the database is cancelled by PostgreSQL, not by hope."
    },
    {
      "id": "MCP_ALLOW_TABLES",
      "envVar": "MCP_ALLOW_TABLES",
      "label": "Comma-separated allowlist",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated allowlist. A table off the list is refused by name, and hiding it inside a CTE, a view or a join does not help."
    },
    {
      "id": "MCP_AUDIT_LOG",
      "envVar": "MCP_AUDIT_LOG",
      "label": "Path to the tamper-evident audit log",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to the tamper-evident audit log. Entries are chained by hash and survive a restart; `--verify-audit` checks the chain against an off-host anchor."
    }
  ]
}
