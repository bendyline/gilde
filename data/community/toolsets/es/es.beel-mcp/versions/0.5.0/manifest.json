{
  "schemaVersion": 1,
  "version": "0.5.0",
  "releasedAt": "2026-08-30T11:06:57.136068Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@beel_es/mcp",
    "version": "0.5.0",
    "sha256": "9ab92f40324a55d1fc3e57007a3ed20d52c11bda27c7d8b07fedac6f7137a5a4",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "BEEL_API_KEY",
      "BEEL_ENV"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "BEEL_API_KEY",
      "envVar": "BEEL_API_KEY",
      "label": "BeeL API key",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "BeeL API key. The prefix selects the environment: beel_sk_test_ for Test, beel_sk_live_ for Live. It may be omitted on a machine where the BeeL CLI has been signed in (`beel login`), whose credentials the server reads from ~/.config/beel/config.json as a fallback; with neither, the server starts and refuses every API call with an actionable error."
    },
    {
      "id": "BEEL_ENV",
      "envVar": "BEEL_ENV",
      "label": "Which stored CLI credential to use when BEEL_API_KEY is not set: test (default) or live",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Which stored CLI credential to use when BEEL_API_KEY is not set: test (default) or live. Ignored when BEEL_API_KEY is set, since the key's prefix already says which environment it belongs to."
    }
  ]
}
