{
  "schemaVersion": 1,
  "version": "1.31.11",
  "releasedAt": "2026-09-11T02:51:21.850966Z",
  "runtime": {
    "kind": "npm-package",
    "package": "webcake-storefront-mcp",
    "version": "1.31.11",
    "sha256": "e65780f215d6e593e693ee2800853dfee8c233f9a4b5dfdf4ac91800fe304483",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "WEBCAKE_TOKEN",
      "WEBCAKE_SESSION_ID",
      "WEBCAKE_ENV",
      "PEXELS_API_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "WEBCAKE_TOKEN",
      "envVar": "WEBCAKE_TOKEN",
      "label": "Storefront JWT Bearer token (the builder's `jwt` cookie / store_jwt)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Storefront JWT Bearer token (the builder's `jwt` cookie / store_jwt)."
    },
    {
      "id": "WEBCAKE_SESSION_ID",
      "envVar": "WEBCAKE_SESSION_ID",
      "label": "Session id sent as the x-session-id header (the builder's `wsid` cookie)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Session id sent as the x-session-id header (the builder's `wsid` cookie)."
    },
    {
      "id": "WEBCAKE_ENV",
      "envVar": "WEBCAKE_ENV",
      "label": "Named environment preset that fills the API + app base URLs (default prod)",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Named environment preset that fills the API + app base URLs (default prod).",
      "options": [
        "local",
        "staging",
        "prod"
      ]
    },
    {
      "id": "PEXELS_API_KEY",
      "envVar": "PEXELS_API_KEY",
      "label": "Optional Pexels API key for the search_images tool",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional Pexels API key for the search_images tool."
    },
    {
      "id": "x-webcake-jwt",
      "envVar": "x-webcake-jwt",
      "label": "Storefront Bearer JWT (or authenticate via the OAuth flow / ?jwt= link)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Storefront Bearer JWT (or authenticate via the OAuth flow / ?jwt= link)."
    },
    {
      "id": "x-webcake-session-id",
      "envVar": "x-webcake-session-id",
      "label": "Session id (the builder's wsid)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Session id (the builder's wsid)."
    }
  ]
}
