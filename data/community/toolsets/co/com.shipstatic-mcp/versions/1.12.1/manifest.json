{
  "schemaVersion": 1,
  "version": "1.12.1",
  "releasedAt": "2026-09-13T07:19:58.895083Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@shipstatic/mcp",
    "version": "1.12.1",
    "sha256": "6960bf2949bd468ea480fd7340d9a06803ae53f402bedc6e14ce815cfb0b095c",
    "entry": "dist/bin.js",
    "args": [],
    "envHints": [
      "SHIP_TOKEN"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SHIP_TOKEN",
      "envVar": "SHIP_TOKEN",
      "label": "Your API key — one credential, two names: the console mints it as an API key (ship-",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Your API key — one credential, two names: the console mints it as an API key (ship-...), and this setting carries it as the token. Free at https://my.shipstatic.com/api-key. Optional: without it, deploys behave like the hosted endpoint (public, claim URL, 3-day expiry). With it, deployments are permanent, the full toolset (custom domains, listing, account-tied ops) unlocks, and rate limits are your own instead of the shared anonymous pools."
    }
  ]
}
