{
  "schemaVersion": 1,
  "version": "2.0.23",
  "releasedAt": "2026-08-12T01:21:48.578234Z",
  "runtime": {
    "kind": "npm-package",
    "package": "argus-decision-mcp",
    "version": "2.0.23",
    "sha256": "f376729f7b1343aa4816bd81fcec7cd9953b6bf91466abe04636d3f7607e68e8",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "ARGUS_DIR",
      "ARGUS_TOKEN",
      "ARGUS_TZ"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "ARGUS_DIR",
      "envVar": "ARGUS_DIR",
      "label": "Directory for the append-only ledger (",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory for the append-only ledger (.argus). Set once and every tool falls back to it. Claude Code expands ${CLAUDE_PROJECT_DIR}; on other hosts use an absolute path."
    },
    {
      "id": "ARGUS_TOKEN",
      "envVar": "ARGUS_TOKEN",
      "label": "Optional Argus account token",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional Argus account token. Set it and saved records can sync so Argus can return on the date you chose. Unset = fully local, no network."
    },
    {
      "id": "ARGUS_TZ",
      "envVar": "ARGUS_TZ",
      "label": "Timezone that decides when a check-by date becomes today (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Timezone that decides when a check-by date becomes today (e.g. Asia/Seoul). Unset = your machine's local timezone."
    }
  ]
}
