{
  "schemaVersion": 1,
  "version": "0.1.5",
  "releasedAt": "2026-07-28T01:48:37.68386Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@numeratica/mcp",
    "version": "0.1.5",
    "sha256": "4c75fd14a6e2f4d4b34f7125db0901eae3a19591ccc42ae243288ecfe29750bd",
    "entry": "bin/numeratica-mcp.js",
    "args": [],
    "envHints": [
      "NUMERATICA_API_KEY",
      "NUMERATICA_BASE_URL",
      "NUMERATICA_TIMEOUT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "NUMERATICA_API_KEY",
      "envVar": "NUMERATICA_API_KEY",
      "label": "Numeratica API key (nmr_sk_",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Numeratica API key (nmr_sk_...). Free self-serve key at https://docs.numeratica.com/get-key"
    },
    {
      "id": "NUMERATICA_BASE_URL",
      "envVar": "NUMERATICA_BASE_URL",
      "label": "Override the API base URL",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the API base URL. Defaults to https://api.numeratica.com; only needed for a staging or self-hosted deployment."
    },
    {
      "id": "NUMERATICA_TIMEOUT_MS",
      "envVar": "NUMERATICA_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds (default 120000, minimum 1000)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds (default 120000, minimum 1000). Raise it for long-horizon Monte Carlo runs."
    },
    {
      "id": "Authorization",
      "envVar": "Authorization",
      "label": "Bearer <your Numeratica API key>, from https://docs",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Bearer <your Numeratica API key>, from https://docs.numeratica.com/get-key. Required for every tools/call. Discovery (initialize, tools/list, ping) answers without it, so a registry can enumerate the catalogue, but a client should still collect the key at setup or the first tool call fails."
    }
  ]
}
