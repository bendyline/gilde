{
  "schemaVersion": 1,
  "version": "0.2.2",
  "releasedAt": "2026-07-22T13:28:25.483617Z",
  "runtime": {
    "kind": "npm-package",
    "package": "cookie-mcp",
    "version": "0.2.2",
    "sha256": "ab9a365d8c3da1bdb2bd3c5e0e7b1e4e780532a7766abff573f029025ed3354b",
    "entry": "dist/mcp/server.js",
    "args": [],
    "envHints": [
      "COOKIE_RPC_URL",
      "COOKIE_PRIVATE_KEY",
      "COOKIE_MAX_TRADE_COOK",
      "COOKIE_SLIPPAGE_BPS",
      "SOLANA_RPC_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "COOKIE_RPC_URL",
      "envVar": "COOKIE_RPC_URL",
      "label": "Cookie Chain RPC endpoint",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Cookie Chain RPC endpoint. Defaults to the public Cookiescan RPC (https://rpc.cookiescan.io)."
    },
    {
      "id": "COOKIE_PRIVATE_KEY",
      "envVar": "COOKIE_PRIVATE_KEY",
      "label": "Wallet key for money-moving tools (base58 secret, solana-keygen JSON array, or a path to a keypair file)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Wallet key for money-moving tools (base58 secret, solana-keygen JSON array, or a path to a keypair file). Optional — without it the server runs read-only and money-moving tools return a clear error. The key is used only to sign locally and is redacted from all output."
    },
    {
      "id": "COOKIE_MAX_TRADE_COOK",
      "envVar": "COOKIE_MAX_TRADE_COOK",
      "label": "Hard per-transaction spend cap in COOK; money-moving tools refuse inputs valued above it (0 disables)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Hard per-transaction spend cap in COOK; money-moving tools refuse inputs valued above it (0 disables). Defaults to 100."
    },
    {
      "id": "COOKIE_SLIPPAGE_BPS",
      "envVar": "COOKIE_SLIPPAGE_BPS",
      "label": "Default slippage tolerance in basis points (500 = 5%)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default slippage tolerance in basis points (500 = 5%). Defaults to 500."
    },
    {
      "id": "SOLANA_RPC_URL",
      "envVar": "SOLANA_RPC_URL",
      "label": "Solana mainnet RPC endpoint, used only by the bridge tool",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Solana mainnet RPC endpoint, used only by the bridge tool. Defaults to the public endpoint, which is rate-limited; a dedicated RPC is recommended for reliable bridging."
    }
  ]
}
