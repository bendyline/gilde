{
  "schemaVersion": 1,
  "version": "1.36.0",
  "releasedAt": "2026-08-25T15:19:01.21774Z",
  "runtime": {
    "kind": "npm-package",
    "package": "tamperlens-mcp",
    "version": "1.36.0",
    "sha256": "1c6e2de746f809610dba67aaa0886efaa2b1960c44e278c6e9d536a71f96a9eb",
    "entry": "dist/mcp/index.js",
    "args": [],
    "envHints": [
      "TAMPERLENS_API_KEY",
      "TAMPERLENS_BASE_URL",
      "TAMPERLENS_ALLOWED_DIRS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "TAMPERLENS_API_KEY",
      "envVar": "TAMPERLENS_API_KEY",
      "label": "Optional",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional. Without it the anonymous allowance applies (10 documents/hour); a free key from tamperlens.com/account raises it. compare_documents requires a key."
    },
    {
      "id": "TAMPERLENS_BASE_URL",
      "envVar": "TAMPERLENS_BASE_URL",
      "label": "Optional",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional. Defaults to the public deployment at https://tamperlens.com."
    },
    {
      "id": "TAMPERLENS_ALLOWED_DIRS",
      "envVar": "TAMPERLENS_ALLOWED_DIRS",
      "label": "Optional",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional. Colon- or comma-separated absolute directories to confine local file reads to. Unset means any absolute path the process can read (the default). Set it to an intake directory so a path argument cannot reach secrets like ~/.ssh or a stray .env; symlinks out of a root are resolved and refused."
    }
  ]
}
