{
  "schemaVersion": 1,
  "version": "12.2.0",
  "releasedAt": "2026-09-10T10:08:41.380721Z",
  "runtime": {
    "kind": "npm-package",
    "package": "brainllm",
    "version": "12.2.0",
    "sha256": "27d7c771292839389af57166b210f250d0e5288c3693509b07a1c5c26914eb18",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "TRILIUM_BASE_URL",
      "TRILIUM_ETAPI_TOKEN",
      "TRILIUM_PASSWORD",
      "BRAINLLM_MODE",
      "BRAINLLM_TZ",
      "BRAINLLM_OWNER_PASSWORD",
      "BRAINLLM_PUBLIC_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "TRILIUM_BASE_URL",
      "envVar": "TRILIUM_BASE_URL",
      "label": "Base URL of the TriliumNext instance, e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Base URL of the TriliumNext instance, e.g. http://localhost:8080"
    },
    {
      "id": "TRILIUM_ETAPI_TOKEN",
      "envVar": "TRILIUM_ETAPI_TOKEN",
      "label": "ETAPI token",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "ETAPI token. Required unless TRILIUM_PASSWORD is set. Create one in Trilium under Options → ETAPI."
    },
    {
      "id": "TRILIUM_PASSWORD",
      "envVar": "TRILIUM_PASSWORD",
      "label": "Alternative to TRILIUM_ETAPI_TOKEN — BrainLLM mints a token on first start and caches it beside brainllm",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Alternative to TRILIUM_ETAPI_TOKEN — BrainLLM mints a token on first start and caches it beside brainllm.json."
    },
    {
      "id": "BRAINLLM_MODE",
      "envVar": "BRAINLLM_MODE",
      "label": "core (default) exposes the 39 brain-aware tools; full additionally exposes the 33 raw ETAPI tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "core (default) exposes the 39 brain-aware tools; full additionally exposes the 33 raw ETAPI tools.",
      "default": "core"
    },
    {
      "id": "BRAINLLM_TZ",
      "envVar": "BRAINLLM_TZ",
      "label": "IANA timezone used to stamp dated notes, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "IANA timezone used to stamp dated notes, e.g. Africa/Johannesburg. Defaults to the host timezone."
    },
    {
      "id": "BRAINLLM_OWNER_PASSWORD",
      "envVar": "BRAINLLM_OWNER_PASSWORD",
      "label": "Enables the OAuth 2",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Enables the OAuth 2.1 / CIMD endpoints required by claude.ai and other hosted Claude surfaces. This is the password entered on the consent screen when authorizing a client."
    },
    {
      "id": "BRAINLLM_PUBLIC_URL",
      "envVar": "BRAINLLM_PUBLIC_URL",
      "label": "Override for the public origin when a proxy rewrites Host",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override for the public origin when a proxy rewrites Host. Must exactly match the URL entered in the client."
    }
  ]
}
