{
  "schemaVersion": 1,
  "version": "0.1.5",
  "releasedAt": "2026-09-14T00:26:48.579482Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@url2md-io/mcp",
    "version": "0.1.5",
    "sha256": "f4597dd57c9dc075645ef6f4bad64c3895ee19feb4acb8e2024e1dd2f67f5999",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "URL2MD_PRIVATE_KEY_FILE",
      "URL2MD_PRIVATE_KEY",
      "URL2MD_MAX_PRICE_USD",
      "URL2MD_MAX_SPEND_USD",
      "URL2MD_BASE_URL",
      "URL2MD_TIMEOUT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "URL2MD_PRIVATE_KEY_FILE",
      "envVar": "URL2MD_PRIVATE_KEY_FILE",
      "label": "Path to a file holding the 0x-prefixed private key of a wallet with USDC on Base",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to a file holding the 0x-prefixed private key of a wallet with USDC on Base. Preferred over URL2MD_PRIVATE_KEY: a path is not a secret, and a key on a command line is visible to every process on the machine. One of the two key variables is required."
    },
    {
      "id": "URL2MD_PRIVATE_KEY",
      "envVar": "URL2MD_PRIVATE_KEY",
      "label": "The key itself: 0x followed by 64 hex characters",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "The key itself: 0x followed by 64 hex characters. Use URL2MD_PRIVATE_KEY_FILE instead where you can. One of the two key variables is required."
    },
    {
      "id": "URL2MD_MAX_PRICE_USD",
      "envVar": "URL2MD_MAX_PRICE_USD",
      "label": "The most this server will commit for one call, in USD, across every payment round of that call",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "The most this server will commit for one call, in USD, across every payment round of that call. A tolled page takes two rounds and this ceiling covers both.",
      "default": "0.10"
    },
    {
      "id": "URL2MD_MAX_SPEND_USD",
      "envVar": "URL2MD_MAX_SPEND_USD",
      "label": "The most this server will commit in total for as long as the process runs",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "The most this server will commit in total for as long as the process runs. Restarting resets it.",
      "default": "1.00"
    },
    {
      "id": "URL2MD_BASE_URL",
      "envVar": "URL2MD_BASE_URL",
      "label": "The url2md deployment to call",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "The url2md deployment to call. Must be https (only a loopback address may be plain http), because this is the origin every 402 is checked against.",
      "default": "https://url2md.io"
    },
    {
      "id": "URL2MD_TIMEOUT_MS",
      "envVar": "URL2MD_TIMEOUT_MS",
      "label": "How long to wait for one conversion, in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "How long to wait for one conversion, in milliseconds. The default covers the service's own worst case: 20 s of fetching plus 55 s of headless render. Do not lower it to 30 s — that is the navigation timeout inside the browser, and on a page that needed a browser your payment has already settled by the time the render starts.",
      "default": "90000"
    }
  ]
}
