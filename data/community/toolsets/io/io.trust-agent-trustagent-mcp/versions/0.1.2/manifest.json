{
  "schemaVersion": 1,
  "version": "0.1.2",
  "releasedAt": "2026-07-28T16:12:46.937322Z",
  "runtime": {
    "kind": "npm-package",
    "package": "trustagent-mcp",
    "version": "0.1.2",
    "sha256": "b297101b8431159caa08f0bc705c40d5db32f2c74878ae0b6dfe294385a3c938",
    "entry": "dist/server.js",
    "args": [],
    "envHints": [
      "TRUSTAGENT_URL",
      "TRUSTAGENT_WALLET_KEY",
      "TRUSTAGENT_MAX_PER_CALL_USD",
      "TRUSTAGENT_MAX_SESSION_USD"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "TRUSTAGENT_URL",
      "envVar": "TRUSTAGENT_URL",
      "label": "Service to query",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Service to query. Defaults to the hosted deployment.",
      "default": "https://trust-agent.io"
    },
    {
      "id": "TRUSTAGENT_WALLET_KEY",
      "envVar": "TRUSTAGENT_WALLET_KEY",
      "label": "Private key of a funded Base wallet",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Private key of a funded Base wallet. Enables the four paid tools, which settle per call in USDC over x402. Omit to run the free tools only — they need no wallet. Requires the optional peer dependency x402-fetch."
    },
    {
      "id": "TRUSTAGENT_MAX_PER_CALL_USD",
      "envVar": "TRUSTAGENT_MAX_PER_CALL_USD",
      "label": "Refuse any single paid call above this amount",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Refuse any single paid call above this amount.",
      "default": "0.05"
    },
    {
      "id": "TRUSTAGENT_MAX_SESSION_USD",
      "envVar": "TRUSTAGENT_MAX_SESSION_USD",
      "label": "Total spend ceiling across all paid calls in one session",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Total spend ceiling across all paid calls in one session. A model looping on a retry is the realistic failure, and a per-call limit does not catch it.",
      "default": "1.00"
    }
  ]
}
