{
  "schemaVersion": 1,
  "version": "0.5.0",
  "releasedAt": "2026-09-14T06:10:20.024876Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@suverselabs/pingpoint-mcp",
    "version": "0.5.0",
    "sha256": "3fbd275bf36e4abe51ef0308675ced2501ac11f556cc5e16eb0cd38f2072eb80",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "PINGPOINT_AGENT_KEY",
      "PINGPOINT_WALLET_KEY_FILE",
      "PINGPOINT_NETWORK",
      "PINGPOINT_BASE_URL",
      "PINGPOINT_RPC_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "PINGPOINT_AGENT_KEY",
      "envVar": "PINGPOINT_AGENT_KEY",
      "label": "PingPoint Agent API key (sup_agent_",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "PingPoint Agent API key (sup_agent_...), issued in the cabinet: Integrations -> Agent API. Either this or PINGPOINT_WALLET_KEY_FILE."
    },
    {
      "id": "PINGPOINT_WALLET_KEY_FILE",
      "envVar": "PINGPOINT_WALLET_KEY_FILE",
      "label": "Pay per call with USDC (x402) instead of a key: path to a file holding the wallet key - EVM private key (0x + 64 hex) for arc-testnet/base-mainnet, solana-keygen JSON or base58 secret for solana-mainnet",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Pay per call with USDC (x402) instead of a key: path to a file holding the wallet key - EVM private key (0x + 64 hex) for arc-testnet/base-mainnet, solana-keygen JSON or base58 secret for solana-mainnet. Either this or PINGPOINT_AGENT_KEY."
    },
    {
      "id": "PINGPOINT_NETWORK",
      "envVar": "PINGPOINT_NETWORK",
      "label": "Network for wallet mode: arc-testnet (default), base-mainnet or solana-mainnet (arc-mainnet once USDC on Arc mainnet is published)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Network for wallet mode: arc-testnet (default), base-mainnet or solana-mainnet (arc-mainnet once USDC on Arc mainnet is published)",
      "default": "arc-testnet"
    },
    {
      "id": "PINGPOINT_BASE_URL",
      "envVar": "PINGPOINT_BASE_URL",
      "label": "Override for the PingPoint API base URL (default https://api",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override for the PingPoint API base URL (default https://api.suverse.io with a key, https://pingpoint.suverse.io with a wallet)"
    },
    {
      "id": "PINGPOINT_RPC_URL",
      "envVar": "PINGPOINT_RPC_URL",
      "label": "Wallet mode only: JSON-RPC URL of the chosen chain for get_balance and, on Solana, the blockhash read each payment needs (default: the public RPC, which rate-limits)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Wallet mode only: JSON-RPC URL of the chosen chain for get_balance and, on Solana, the blockhash read each payment needs (default: the public RPC, which rate-limits)"
    }
  ]
}
