{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-07-20T21:38:24.754086Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/provenance-mcp",
    "version": "0.1.1",
    "sha256": "84768a10cb2ec66fc58f17286760e86db371142fd206387a91ccb648ca95b681",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS",
      "THREE_WS_TOKEN",
      "THREE_WS_SIGNER_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API that serves the agent-actions ledger. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds for the ledger reads/appends",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds for the ledger reads/appends.",
      "default": "20000"
    },
    {
      "id": "THREE_WS_TOKEN",
      "envVar": "THREE_WS_TOKEN",
      "label": "Bearer token authenticating every call — a three",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Bearer token authenticating every call — a three.ws API key (sk_live_…) or an OAuth access token. The ledger is owner-scoped: you can read and append actions only for agents your token owns. Required for all three tools. Treat like a password."
    },
    {
      "id": "THREE_WS_SIGNER_KEY",
      "envVar": "THREE_WS_SIGNER_KEY",
      "label": "Optional 0x-prefixed EVM private key",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional 0x-prefixed EVM private key. When set, append_agent_action ERC-191-signs each action so anyone can ecrecover the signer and verify authorship on-chain. A per-call `signer_key` arg overrides it. Omit to append unsigned. Treat like cash."
    }
  ]
}
