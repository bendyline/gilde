{
  "schemaVersion": 1,
  "version": "0.5.0",
  "releasedAt": "2026-09-07T19:14:05.726322Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@ni-c/imap-mcp",
    "version": "0.5.0",
    "sha256": "322870ab18350bf256dcaa43c088a5912404131e0ef3903fe3ea5834972f5988",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "IMAP_HOST",
      "IMAP_USER",
      "IMAP_PASSWORD",
      "IMAP_PORT",
      "IMAP_TLS",
      "IMAP_MAILBOX",
      "IMAP_SEEN_KEYWORD",
      "IMAP_READ_ONLY",
      "IMAP_ALLOW_TOOLS",
      "IMAP_DENY_TOOLS",
      "IMAP_DOWNLOAD_DIR",
      "IMAP_INSECURE_TLS",
      "IMAP_DRAFTS_MAILBOX",
      "IMAP_MAX_MESSAGES",
      "IMAP_MAX_ATTACHMENT_BYTES",
      "IMAP_MAX_DOWNLOAD_BYTES",
      "IMAP_MAX_EXTRACT_BYTES",
      "IMAP_ATTACHMENT_TYPES",
      "IMAP_TRUSTED_AUTHSERV_ID"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "IMAP_HOST",
      "envVar": "IMAP_HOST",
      "label": "Hostname of the IMAP server, e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Hostname of the IMAP server, e.g. imap.example.net"
    },
    {
      "id": "IMAP_USER",
      "envVar": "IMAP_USER",
      "label": "Mailbox user name",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Mailbox user name"
    },
    {
      "id": "IMAP_PASSWORD",
      "envVar": "IMAP_PASSWORD",
      "label": "Mailbox password or app password",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Mailbox password or app password"
    },
    {
      "id": "IMAP_PORT",
      "envVar": "IMAP_PORT",
      "label": "Defaults to 993 with implicit TLS, 143 otherwise",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Defaults to 993 with implicit TLS, 143 otherwise"
    },
    {
      "id": "IMAP_TLS",
      "envVar": "IMAP_TLS",
      "label": "implicit (default), starttls or none",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "implicit (default), starttls or none"
    },
    {
      "id": "IMAP_MAILBOX",
      "envVar": "IMAP_MAILBOX",
      "label": "Default mailbox to work in (default: INBOX)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default mailbox to work in (default: INBOX)"
    },
    {
      "id": "IMAP_SEEN_KEYWORD",
      "envVar": "IMAP_SEEN_KEYWORD",
      "label": "IMAP keyword marking what the assistant has already seen (default: AiSeen)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "IMAP keyword marking what the assistant has already seen (default: AiSeen)"
    },
    {
      "id": "IMAP_READ_ONLY",
      "envVar": "IMAP_READ_ONLY",
      "label": "Set to false to register the mailbox write tools; defaults to true",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to false to register the mailbox write tools; defaults to true"
    },
    {
      "id": "IMAP_ALLOW_TOOLS",
      "envVar": "IMAP_ALLOW_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)"
    },
    {
      "id": "IMAP_DENY_TOOLS",
      "envVar": "IMAP_DENY_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to remove from whatever IMAP_ALLOW_TOOLS left",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to remove from whatever IMAP_ALLOW_TOOLS left"
    },
    {
      "id": "IMAP_DOWNLOAD_DIR",
      "envVar": "IMAP_DOWNLOAD_DIR",
      "label": "Directory attachments may be written to; unset means the server never touches the filesystem",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory attachments may be written to; unset means the server never touches the filesystem"
    },
    {
      "id": "IMAP_INSECURE_TLS",
      "envVar": "IMAP_INSECURE_TLS",
      "label": "Set to true to accept self-signed certificates, scoped to this connection",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to accept self-signed certificates, scoped to this connection"
    },
    {
      "id": "IMAP_DRAFTS_MAILBOX",
      "envVar": "IMAP_DRAFTS_MAILBOX",
      "label": "Drafts folder to save into; by default the one flagged \\Drafts is used",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Drafts folder to save into; by default the one flagged \\Drafts is used"
    },
    {
      "id": "IMAP_MAX_MESSAGES",
      "envVar": "IMAP_MAX_MESSAGES",
      "label": "Default number of messages a listing returns (default: 100)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default number of messages a listing returns (default: 100)"
    },
    {
      "id": "IMAP_MAX_ATTACHMENT_BYTES",
      "envVar": "IMAP_MAX_ATTACHMENT_BYTES",
      "label": "Ceiling for returning an attachment inline (default: 1048576)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Ceiling for returning an attachment inline (default: 1048576)"
    },
    {
      "id": "IMAP_MAX_DOWNLOAD_BYTES",
      "envVar": "IMAP_MAX_DOWNLOAD_BYTES",
      "label": "Ceiling for writing an attachment to disk (default: 26214400)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Ceiling for writing an attachment to disk (default: 26214400)"
    },
    {
      "id": "IMAP_MAX_EXTRACT_BYTES",
      "envVar": "IMAP_MAX_EXTRACT_BYTES",
      "label": "Ceiling for reading a document's text with mode=text (default: 10485760, maximum: 67108864)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Ceiling for reading a document's text with mode=text (default: 10485760, maximum: 67108864)"
    },
    {
      "id": "IMAP_ATTACHMENT_TYPES",
      "envVar": "IMAP_ATTACHMENT_TYPES",
      "label": "Comma-separated content-type allowlist for attachments",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated content-type allowlist for attachments"
    },
    {
      "id": "IMAP_TRUSTED_AUTHSERV_ID",
      "envVar": "IMAP_TRUSTED_AUTHSERV_ID",
      "label": "authserv-id your provider stamps into Authentication-Results; without it every verdict is reported as forgeable",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "authserv-id your provider stamps into Authentication-Results; without it every verdict is reported as forgeable"
    }
  ]
}
