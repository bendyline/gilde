{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-07-20T21:38:25.496885Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/signals-mcp",
    "version": "0.1.1",
    "sha256": "47a95928d2aba647a10a68cab0b7b2b560ef13d89ef4d0f07f320c67b4fcf758",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS",
      "THREE_WS_API_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API that serves the signal-marketplace endpoints. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds for the live marketplace reads and subscription writes",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds for the live marketplace reads and subscription writes.",
      "default": "20000"
    },
    {
      "id": "THREE_WS_API_KEY",
      "envVar": "THREE_WS_API_KEY",
      "label": "three",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "three.ws API key (sk_live_… / sk_test_…) for the account-scoped follower tools (get_subscriptions, subscribe_signal, set_subscription_status). Optional — the public discovery and leaderboard tools work without it. subscribe_signal in live mode spends real USDC, so treat this key like cash."
    }
  ]
}
