{
  "schemaVersion": 1,
  "version": "0.1.3",
  "releasedAt": "2026-09-08T13:32:33.605165Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@ni-c/carddav-mcp",
    "version": "0.1.3",
    "sha256": "e7f6b23dba39d988e91afc1d03c6c411c3ed0048fa525f3d88d6c699df0130f5",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "CARDDAV_URL",
      "CARDDAV_USERNAME",
      "CARDDAV_PASSWORD",
      "CARDDAV_TOKEN",
      "CARDDAV_ADDRESSBOOKS",
      "CARDDAV_MAX_CONTACTS",
      "CARDDAV_READ_ONLY",
      "CARDDAV_INSECURE_TLS",
      "CARDDAV_ALLOW_PLAINTEXT",
      "CARDDAV_ALLOW_TOOLS",
      "CARDDAV_DENY_TOOLS",
      "ELICITATION"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "CARDDAV_URL",
      "envVar": "CARDDAV_URL",
      "label": "Root of the CardDAV server, e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Root of the CardDAV server, e.g. https://dav.example.net. An address book collection URL also works."
    },
    {
      "id": "CARDDAV_USERNAME",
      "envVar": "CARDDAV_USERNAME",
      "label": "Account name",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Account name. Most hosted services want an app-specific password with it."
    },
    {
      "id": "CARDDAV_PASSWORD",
      "envVar": "CARDDAV_PASSWORD",
      "label": "Password or app-specific password",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Password or app-specific password. Deleted from the environment once read."
    },
    {
      "id": "CARDDAV_TOKEN",
      "envVar": "CARDDAV_TOKEN",
      "label": "Bearer token, as an alternative to username and password",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Bearer token, as an alternative to username and password. Not both."
    },
    {
      "id": "CARDDAV_ADDRESSBOOKS",
      "envVar": "CARDDAV_ADDRESSBOOKS",
      "label": "Comma-separated address books this server may touch, by path or final path segment",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated address books this server may touch, by path or final path segment. Default: all of them."
    },
    {
      "id": "CARDDAV_MAX_CONTACTS",
      "envVar": "CARDDAV_MAX_CONTACTS",
      "label": "Contacts a listing returns by default, 1 to 500 (default: 100)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Contacts a listing returns by default, 1 to 500 (default: 100)"
    },
    {
      "id": "CARDDAV_READ_ONLY",
      "envVar": "CARDDAV_READ_ONLY",
      "label": "Set to true to register only the read tools (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to register only the read tools (default: false)"
    },
    {
      "id": "CARDDAV_INSECURE_TLS",
      "envVar": "CARDDAV_INSECURE_TLS",
      "label": "Set to true to accept a self-signed certificate on the configured host only (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to accept a self-signed certificate on the configured host only (default: false)"
    },
    {
      "id": "CARDDAV_ALLOW_PLAINTEXT",
      "envVar": "CARDDAV_ALLOW_PLAINTEXT",
      "label": "Set to true to allow a plain http:// URL to a host that is not loopback, which sends the credentials and every contact unencrypted (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to allow a plain http:// URL to a host that is not loopback, which sends the credentials and every contact unencrypted (default: false)"
    },
    {
      "id": "CARDDAV_ALLOW_TOOLS",
      "envVar": "CARDDAV_ALLOW_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)"
    },
    {
      "id": "CARDDAV_DENY_TOOLS",
      "envVar": "CARDDAV_DENY_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to remove from whatever the allow list left",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to remove from whatever the allow list left"
    },
    {
      "id": "ELICITATION",
      "envVar": "ELICITATION",
      "label": "Deliberately unprefixed, and reaches every MCP server in the environment",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Deliberately unprefixed, and reaches every MCP server in the environment. Set to false to make guarded tools use the two-call token instead of a dialog (default: true)"
    }
  ]
}
