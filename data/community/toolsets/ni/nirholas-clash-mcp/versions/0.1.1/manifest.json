{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-07-20T21:38:29.761642Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/clash-mcp",
    "version": "0.1.1",
    "sha256": "4a24a51d65e68a6e9dfabdc764b6aca5352fdd9e35ca37f21da7aff1a96fa2cf",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS",
      "SOLANA_SECRET_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API that serves the Coin Clash endpoints. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds for the live Coin Clash reads and writes",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds for the live Coin Clash reads and writes.",
      "default": "20000"
    },
    {
      "id": "SOLANA_SECRET_KEY",
      "envVar": "SOLANA_SECRET_KEY",
      "label": "Base58 Solana secret key for the enlisting wallet",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Base58 Solana secret key for the enlisting wallet. Required only for the write tools (enlist_faction, rally_faction) — reads work without it. Per-call `secret` arguments override this. The wallet must hold the faction coin it enlists for. Treat like cash."
    }
  ]
}
