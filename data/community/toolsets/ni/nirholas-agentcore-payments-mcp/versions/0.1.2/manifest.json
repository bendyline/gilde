{
  "schemaVersion": 1,
  "version": "0.1.2",
  "releasedAt": "2026-07-20T21:38:31.505774Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/agentcore-payments-mcp",
    "version": "0.1.2",
    "sha256": "05031fa0af1b92f0f94b190a4b521ebe72fcab795788db3e98b04c7dd6845878",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_SESSION",
      "PAYMENT_SESSION_TOKEN",
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_SESSION",
      "envVar": "THREE_WS_SESSION",
      "label": "Your three",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Your three.ws session token (value of the `__Host-sid` cookie from a signed-in browser). Required for account management operations: create_payment_session, list_payment_sessions, check_payment_session, cancel_payment_session."
    },
    {
      "id": "PAYMENT_SESSION_TOKEN",
      "envVar": "PAYMENT_SESSION_TOKEN",
      "label": "A payment session bearer token (starts with 'pss_') from create_payment_session",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "A payment session bearer token (starts with 'pss_') from create_payment_session. Used by the agent for pay_with_session calls when no inline token is passed. Treat like a spending card — it authorizes payments up to the session's budget."
    },
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request HTTP timeout in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request HTTP timeout in milliseconds. x402 payments involve on-chain settlement so this should be generous (default: 30000).",
      "default": "30000"
    }
  ]
}
