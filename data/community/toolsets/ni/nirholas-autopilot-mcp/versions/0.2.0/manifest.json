{
  "schemaVersion": 1,
  "version": "0.2.0",
  "releasedAt": "2026-06-24T09:41:18.570013Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/autopilot-mcp",
    "version": "0.2.0",
    "sha256": "cd1fca214f0fcc0c21a73a6c26f61424206edb00afaa44cdbb21f621e4b19fb1",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_API_KEY",
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_API_KEY",
      "envVar": "THREE_WS_API_KEY",
      "label": "REQUIRED",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "REQUIRED. The agent owner's three.ws credential — a three.ws API key (sk_live_… / sk_test_…) or an OAuth access token. Sent as Authorization: Bearer on every request; every autopilot endpoint is owner-scoped and rejects unauthenticated calls. This credential can move real SOL via execute_proposal — treat it like a password. THREE_WS_TOKEN / THREE_WS_BEARER are accepted aliases."
    },
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API that serves the autopilot endpoints. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds. Proposal generation runs a real server-side LLM pass, so the default is generous.",
      "default": "60000"
    }
  ]
}
