{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-07-20T21:38:25.907989Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/alerts-mcp",
    "version": "0.1.1",
    "sha256": "ae1f5c3e3b129f019a2be907ede59211565909408db5034485d257b45b208cdb",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS",
      "THREE_WS_SESSION"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API that serves the alert-rules and notifications endpoints. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds for the live three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds for the live three.ws API reads and writes.",
      "default": "20000"
    },
    {
      "id": "THREE_WS_SESSION",
      "envVar": "THREE_WS_SESSION",
      "label": "Your three",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Your three.ws session token (the value of the `__Host-sid` cookie from a signed-in browser). Required by every tool — all rules and alerts are scoped to YOUR account. Writes also use it to fetch a one-time CSRF token. Treat like a password."
    }
  ]
}
