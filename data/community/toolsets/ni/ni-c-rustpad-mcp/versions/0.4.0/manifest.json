{
  "schemaVersion": 1,
  "version": "0.4.0",
  "releasedAt": "2026-09-07T17:57:07.084637Z",
  "runtime": {
    "kind": "npm-package",
    "package": "rustpad-mcp",
    "version": "0.4.0",
    "sha256": "db778f70c23019a220d6c149ce41c32e35a650f27dd27bf71a4b1e0fcf7a7f0a",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "RUSTPAD_URL",
      "RUSTPAD_READ_ONLY",
      "RUSTPAD_INSECURE_TLS",
      "RUSTPAD_ALLOW_TOOLS",
      "RUSTPAD_DENY_TOOLS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "RUSTPAD_URL",
      "envVar": "RUSTPAD_URL",
      "label": "Base URL of the Rustpad instance, e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Base URL of the Rustpad instance, e.g. https://rustpad.example.net"
    },
    {
      "id": "RUSTPAD_READ_ONLY",
      "envVar": "RUSTPAD_READ_ONLY",
      "label": "Exactly 'true' registers only the 3 read tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Exactly 'true' registers only the 3 read tools"
    },
    {
      "id": "RUSTPAD_INSECURE_TLS",
      "envVar": "RUSTPAD_INSECURE_TLS",
      "label": "Exactly 'true' accepts a self-signed certificate, scoped to this connection",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Exactly 'true' accepts a self-signed certificate, scoped to this connection"
    },
    {
      "id": "RUSTPAD_ALLOW_TOOLS",
      "envVar": "RUSTPAD_ALLOW_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)"
    },
    {
      "id": "RUSTPAD_DENY_TOOLS",
      "envVar": "RUSTPAD_DENY_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to remove from whatever RUSTPAD_ALLOW_TOOLS left",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to remove from whatever RUSTPAD_ALLOW_TOOLS left"
    }
  ]
}
