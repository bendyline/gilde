{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-07-20T21:38:26.931763Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@three-ws/billing-mcp",
    "version": "0.1.1",
    "sha256": "ced8e02512e096f948f2bdc2a73c437de456568b98db2a38323f507ee2d02049",
    "entry": "src/index.js",
    "args": [],
    "envHints": [
      "THREE_WS_BASE",
      "THREE_WS_TIMEOUT_MS",
      "THREE_WS_SESSION"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "THREE_WS_BASE",
      "envVar": "THREE_WS_BASE",
      "label": "Base URL of the three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the three.ws API that serves the billing endpoints. Override only when self-hosting or targeting a preview deployment.",
      "default": "https://three.ws"
    },
    {
      "id": "THREE_WS_TIMEOUT_MS",
      "envVar": "THREE_WS_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds for the live three",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds for the live three.ws API reads.",
      "default": "20000"
    },
    {
      "id": "THREE_WS_SESSION",
      "envVar": "THREE_WS_SESSION",
      "label": "Your three",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Your three.ws session token (the value of the `__Host-sid` cookie from a signed-in browser). REQUIRED — it exposes private billing data, so every account-scoped read (get_billing_summary, query_usage, export_billing_history, get_receipt, get_revenue) resolves the calling user from it and returns only your data. Only the public get_fee_info works without it. Treat like a password."
    }
  ]
}
