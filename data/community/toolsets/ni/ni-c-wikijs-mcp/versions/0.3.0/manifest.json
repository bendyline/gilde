{
  "schemaVersion": 1,
  "version": "0.3.0",
  "releasedAt": "2026-09-07T15:03:45.890937Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@ni-c/wikijs-mcp",
    "version": "0.3.0",
    "sha256": "793f5d0effc2f3d507c5f480f2f72a241717be6c4dc81c90d97c61ede675f31f",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "WIKIJS_URL",
      "WIKIJS_TOKEN",
      "WIKIJS_LOCALE",
      "WIKIJS_READ_ONLY",
      "WIKIJS_ALLOWED_PATHS",
      "WIKIJS_ALLOW_TOOLS",
      "WIKIJS_DENY_TOOLS",
      "WIKIJS_INSECURE_TLS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "WIKIJS_URL",
      "envVar": "WIKIJS_URL",
      "label": "Base URL of the Wiki",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Base URL of the Wiki.js instance, e.g. https://wiki.example.com"
    },
    {
      "id": "WIKIJS_TOKEN",
      "envVar": "WIKIJS_TOKEN",
      "label": "API key from Administration → API Access",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "API key from Administration → API Access. WIKIJS_API_KEY is accepted as an alias"
    },
    {
      "id": "WIKIJS_LOCALE",
      "envVar": "WIKIJS_LOCALE",
      "label": "Locale assumed by page tools that are not given one; part of a page's identity (default: en)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Locale assumed by page tools that are not given one; part of a page's identity (default: en)"
    },
    {
      "id": "WIKIJS_READ_ONLY",
      "envVar": "WIKIJS_READ_ONLY",
      "label": "Set to true to register only the read tools (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to register only the read tools (default: false)"
    },
    {
      "id": "WIKIJS_ALLOWED_PATHS",
      "envVar": "WIKIJS_ALLOWED_PATHS",
      "label": "Comma-separated page path prefixes the write tools are confined to, matched by path segment",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated page path prefixes the write tools are confined to, matched by path segment"
    },
    {
      "id": "WIKIJS_ALLOW_TOOLS",
      "envVar": "WIKIJS_ALLOW_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to register; 'essential' selects a curated preset (default: all tools)"
    },
    {
      "id": "WIKIJS_DENY_TOOLS",
      "envVar": "WIKIJS_DENY_TOOLS",
      "label": "Comma-separated tool names or list_* prefixes to remove from whatever the allow list left",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool names or list_* prefixes to remove from whatever the allow list left"
    },
    {
      "id": "WIKIJS_INSECURE_TLS",
      "envVar": "WIKIJS_INSECURE_TLS",
      "label": "Set to true to accept a self-signed certificate from a self-hosted instance (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to accept a self-signed certificate from a self-hosted instance (default: false)"
    }
  ]
}
