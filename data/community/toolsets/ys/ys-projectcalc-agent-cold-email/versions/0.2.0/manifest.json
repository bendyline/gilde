{
  "schemaVersion": 1,
  "version": "0.2.0",
  "releasedAt": "2026-07-22T12:21:30.58105Z",
  "runtime": {
    "kind": "npm-package",
    "package": "agent-cold-email",
    "version": "0.2.0",
    "sha256": "28cadb56bc9cfc32d737ed8f9524953372fb90d2e70c9ffbbed1de06476dd55c",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "AGENT_COLD_EMAIL_API_KEY",
      "AGENT_COLD_EMAIL_BASE_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "AGENT_COLD_EMAIL_API_KEY",
      "envVar": "AGENT_COLD_EMAIL_API_KEY",
      "label": "Bearer token from POST /signup (free, no card) — or run `npx agent-cold-email demo` first",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Bearer token from POST /signup (free, no card) — or run `npx agent-cold-email demo` first. Optional at startup: without it, `initialize`/`tools/list` still work (unauthenticated introspection), but every `tools/call` fails until a key is set."
    },
    {
      "id": "AGENT_COLD_EMAIL_BASE_URL",
      "envVar": "AGENT_COLD_EMAIL_BASE_URL",
      "label": "Override the API base URL (default: https://agent-cold-email-api",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the API base URL (default: https://agent-cold-email-api.yaakovscher.workers.dev)"
    },
    {
      "id": "Authorization",
      "envVar": "Authorization",
      "label": "Bearer token from POST /signup (free, no card) — or run `npx agent-cold-email demo` first",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Bearer token from POST /signup (free, no card) — or run `npx agent-cold-email demo` first"
    }
  ]
}
