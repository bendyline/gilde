{
  "schemaVersion": 1,
  "version": "0.2.5",
  "releasedAt": "2026-08-29T05:51:31.528839Z",
  "runtime": {
    "kind": "npm-package",
    "package": "skim-mcp",
    "version": "0.2.5",
    "sha256": "767f5f3b00a7a6c7d969bd04a423a712a2c357d5f58646913e8093665b038bae",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "SKIM_API_KEY",
      "SKIM_WALLET_PRIVATE_KEY",
      "SKIM_MAX_PRICE_USD"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SKIM_API_KEY",
      "envVar": "SKIM_API_KEY",
      "label": "Recommended",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Recommended. Card-plan API key (sk402_...). Get a free key at https://skim402.com/pricing. Takes priority over SKIM_WALLET_PRIVATE_KEY. Enables read_url, read_urls, extract_url, crawl_url, read_pdf, watch, and poll_signal without a wallet."
    },
    {
      "id": "SKIM_WALLET_PRIVATE_KEY",
      "envVar": "SKIM_WALLET_PRIVATE_KEY",
      "label": "Optional",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional. Base-network wallet private key (0x-prefixed, 64 hex chars) for x402 pay-per-call if you are not using SKIM_API_KEY. Fund with a small amount of USDC on Base. Ignored when SKIM_API_KEY is set."
    },
    {
      "id": "SKIM_MAX_PRICE_USD",
      "envVar": "SKIM_MAX_PRICE_USD",
      "label": "Optional cap on per-call price in USD (default 0",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional cap on per-call price in USD (default 0.01). Wallet lane only. Raise (e.g. 0.05) for batch/extract/watch. Requests that would cost more than this are rejected without payment."
    },
    {
      "id": "X-Skim-Wallet-Key",
      "envVar": "X-Skim-Wallet-Key",
      "label": "Optional",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional. Without it, skim_read works on the shared free tier. To pay per call via x402 wallet: add a Base wallet private key (fresh, low-balance agent wallet) — unlocks paid tools at $0.002/$0.005/$0.015. Card plan alternative (API key): https://skim402.com/pricing"
    }
  ]
}
