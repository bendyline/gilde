{
  "schemaVersion": 1,
  "version": "0.3.0",
  "releasedAt": "2026-08-19T11:50:33.625269Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@infino-ai/code-context",
    "version": "0.3.0",
    "sha256": "52dd25ed6a4b9170403cd2122cfb9ea4fd6fd6e0e41892526275c1ff7f1ac2c8",
    "entry": "dist/cli.js",
    "args": [],
    "envHints": [
      "CX_ROOT",
      "CX_INDEX_DIR",
      "CX_AUTO_SYNC",
      "CX_NO_EMBED",
      "CX_NO_RECEIPT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "CX_ROOT",
      "envVar": "CX_ROOT",
      "label": "Repository root to serve",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Repository root to serve. Optional - defaults to the working directory the server is launched in."
    },
    {
      "id": "CX_INDEX_DIR",
      "envVar": "CX_INDEX_DIR",
      "label": "Where the index lives",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Where the index lives. Optional - defaults to .infino/ inside the repository root."
    },
    {
      "id": "CX_AUTO_SYNC",
      "envVar": "CX_AUTO_SYNC",
      "label": "Set to 0 to disable the background staleness sync that runs as queries arrive",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 0 to disable the background staleness sync that runs as queries arrive."
    },
    {
      "id": "CX_NO_EMBED",
      "envVar": "CX_NO_EMBED",
      "label": "Set (1/true/yes) for keyword-only mode - skip the vector stage entirely",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set (1/true/yes) for keyword-only mode - skip the vector stage entirely."
    },
    {
      "id": "CX_NO_RECEIPT",
      "envVar": "CX_NO_RECEIPT",
      "label": "Set (1/true/yes) to turn off local usage accounting: the per-call receipt on results and the cx usage ledger",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set (1/true/yes) to turn off local usage accounting: the per-call receipt on results and the cx usage ledger. Optional - on by default, computed locally, nothing leaves the machine."
    }
  ]
}
