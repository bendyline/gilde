{
  "schemaVersion": 1,
  "version": "1.0.2",
  "releasedAt": "2026-05-06T14:54:00.068039Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@intent-driven/mcp-server",
    "version": "1.0.2",
    "sha256": "647e4feb29763064c3638352f3d4774624531f8fe9ec838d46ca9e7535b15d7d",
    "entry": "bin/mcp-idf.js",
    "args": [],
    "envHints": [
      "IDF_SERVER",
      "IDF_DOMAIN",
      "IDF_BOOTSTRAP",
      "IDF_ONTOLOGY_PATH",
      "IDF_AUTH_TOKEN"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "IDF_SERVER",
      "envVar": "IDF_SERVER",
      "label": "Base URL of the IDF host runtime (e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Base URL of the IDF host runtime (e.g. http://localhost:3001)."
    },
    {
      "id": "IDF_DOMAIN",
      "envVar": "IDF_DOMAIN",
      "label": "IDF domain to expose as MCP tools (e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "IDF domain to expose as MCP tools (e.g. invest, booking, planning)."
    },
    {
      "id": "IDF_BOOTSTRAP",
      "envVar": "IDF_BOOTSTRAP",
      "label": "Set to 0 if the host runtime already bootstrapped the domain; 1 (default) to bootstrap from IDF_ONTOLOGY_PATH on first connection",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 0 if the host runtime already bootstrapped the domain; 1 (default) to bootstrap from IDF_ONTOLOGY_PATH on first connection."
    },
    {
      "id": "IDF_ONTOLOGY_PATH",
      "envVar": "IDF_ONTOLOGY_PATH",
      "label": "Filesystem path to the domain ontology module (only used when IDF_BOOTSTRAP=1)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Filesystem path to the domain ontology module (only used when IDF_BOOTSTRAP=1)."
    },
    {
      "id": "IDF_AUTH_TOKEN",
      "envVar": "IDF_AUTH_TOKEN",
      "label": "Optional bearer token forwarded to the host runtime as the agent's role credential",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional bearer token forwarded to the host runtime as the agent's role credential."
    }
  ]
}
