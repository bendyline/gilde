{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-06-28T03:17:16.906997Z",
  "runtime": {
    "kind": "npm-package",
    "package": "account-pool-mcp",
    "version": "0.1.1",
    "sha256": "1538c32ef2355ea42ddaf38bcf4a9b079bbde079a98bdeab9d1735bc3f1b08b4",
    "entry": "dist/cli.js",
    "args": [],
    "envHints": [
      "APM_ACCOUNTS_FILE",
      "APM_DB_PATH",
      "APM_DEFAULT_TTL_SECONDS",
      "APM_LEASE_WAIT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "APM_ACCOUNTS_FILE",
      "envVar": "APM_ACCOUNTS_FILE",
      "label": "Path to the JSON file defining pools + accounts to load on startup",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to the JSON file defining pools + accounts to load on startup.",
      "default": "./accounts.json"
    },
    {
      "id": "APM_DB_PATH",
      "envVar": "APM_DB_PATH",
      "label": "Path to the SQLite file",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to the SQLite file. Every session that should coordinate must point at the SAME path.",
      "default": "./account-pool.db"
    },
    {
      "id": "APM_DEFAULT_TTL_SECONDS",
      "envVar": "APM_DEFAULT_TTL_SECONDS",
      "label": "How long a lease lasts before it is reclaimable if a session crashes without releasing",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "How long a lease lasts before it is reclaimable if a session crashes without releasing.",
      "default": "1800"
    },
    {
      "id": "APM_LEASE_WAIT_MS",
      "envVar": "APM_LEASE_WAIT_MS",
      "label": "0 = fail fast when the pool is empty; >0 = wait this many ms for an account to free up",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "0 = fail fast when the pool is empty; >0 = wait this many ms for an account to free up.",
      "default": "0"
    }
  ]
}
