{
  "schemaVersion": 1,
  "version": "0.1.12",
  "releasedAt": "2026-09-07T10:43:53.518133Z",
  "runtime": {
    "kind": "npm-package",
    "package": "ossfind",
    "version": "0.1.12",
    "sha256": "0019ff5feaef132096fd4c5755818d77199624e018b22c690ad95adf000d14ee",
    "entry": "bin/ossfind",
    "args": [],
    "envHints": [
      "GITHUB_TOKEN",
      "LIBRARY_IO_API_KEY",
      "OSSFIND_FIXTURES"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "GITHUB_TOKEN",
      "envVar": "GITHUB_TOKEN",
      "label": "Optional",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional. Raises the GitHub repository-search rate limit; discovery works without it."
    },
    {
      "id": "LIBRARY_IO_API_KEY",
      "envVar": "LIBRARY_IO_API_KEY",
      "label": "Optional libraries",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional libraries.io key, used as the PyPI discovery fallback when no local index has been built. Without it, and without an index, PyPI search reports itself unavailable rather than returning nothing."
    },
    {
      "id": "OSSFIND_FIXTURES",
      "envVar": "OSSFIND_FIXTURES",
      "label": "Set to 1 to run fully offline against frozen supplier responses, for evaluation without network access",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 1 to run fully offline against frozen supplier responses, for evaluation without network access."
    }
  ]
}
