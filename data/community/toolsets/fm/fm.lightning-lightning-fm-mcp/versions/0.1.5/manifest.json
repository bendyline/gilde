{
  "schemaVersion": 1,
  "version": "0.1.5",
  "releasedAt": "2026-08-20T15:10:04.207629Z",
  "runtime": {
    "kind": "npm-package",
    "package": "lightning-fm-mcp",
    "version": "0.1.5",
    "sha256": "a840655d40664f79936cc51704777fa0a56d29330c116c084e4bd3fd33662d4b",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "LFM_NOSTR_RELAYS",
      "LFM_BLOSSOM_URL",
      "LFM_CACHE_TTL_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "LFM_NOSTR_RELAYS",
      "envVar": "LFM_NOSTR_RELAYS",
      "label": "Comma-separated relay URLs to query",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated relay URLs to query. Only add a relay here if it gates writes the way relay.lightning.fm does (allowlisted artists only) — an unrestricted public relay hands back every other app's kind-31337 events too, not just Lightning FM's.",
      "default": "wss://relay.lightning.fm"
    },
    {
      "id": "LFM_BLOSSOM_URL",
      "envVar": "LFM_BLOSSOM_URL",
      "label": "Blossom server audio/image hashes resolve against",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Blossom server audio/image hashes resolve against.",
      "default": "https://media.lightning.fm"
    },
    {
      "id": "LFM_CACHE_TTL_MS",
      "envVar": "LFM_CACHE_TTL_MS",
      "label": "How long a fetched catalog/profile/now-playing snapshot is reused, in milliseconds, before the next tool call re-queries the relays",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "How long a fetched catalog/profile/now-playing snapshot is reused, in milliseconds, before the next tool call re-queries the relays.",
      "default": "60000"
    }
  ]
}
