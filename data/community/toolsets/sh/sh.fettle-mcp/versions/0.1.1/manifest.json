{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-08-25T07:13:11.362162Z",
  "runtime": {
    "kind": "npm-package",
    "package": "fettle-mcp",
    "version": "0.1.1",
    "sha256": "021e5a5e338618b2440ee7010f863008d2e8ae630bc9e1b81a61e069ade02587",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "FETTLE_API_KEY",
      "FETTLE_API_URL",
      "FETTLE_TIMEOUT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "FETTLE_API_KEY",
      "envVar": "FETTLE_API_KEY",
      "label": "Fettle API key (Settings → API Keys at https://fettle",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Fettle API key (Settings → API Keys at https://fettle.sh). Read scope lists and inspects; write scope is needed to create, change or pause anything."
    },
    {
      "id": "FETTLE_API_URL",
      "envVar": "FETTLE_API_URL",
      "label": "Base URL of the Fettle API",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the Fettle API. Only set this to point the server at something other than production.",
      "default": "https://api.fettle.sh"
    },
    {
      "id": "FETTLE_TIMEOUT_MS",
      "envVar": "FETTLE_TIMEOUT_MS",
      "label": "How long one tool call may take before it is abandoned",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "How long one tool call may take before it is abandoned.",
      "default": "30000"
    },
    {
      "id": "Authorization",
      "envVar": "Authorization",
      "label": "Fettle API key as a bearer token: \"Bearer fettle_…\"",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Fettle API key as a bearer token: \"Bearer fettle_…\". The same key the npm package takes in FETTLE_API_KEY."
    }
  ]
}
