{
  "schemaVersion": 1,
  "version": "0.14.2",
  "releasedAt": "2026-09-06T08:35:04.485076Z",
  "runtime": {
    "kind": "npm-package",
    "package": "sub-agents-mcp",
    "version": "0.14.2",
    "sha256": "615931a8e23b295dd8e78d8eca7886832d91e896fb5d6dc5e3b84b9e47f19813",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "AGENTS_DIR",
      "AGENT_TYPE",
      "AGENT_PERMISSION",
      "AGENT_MODEL",
      "AGENT_EFFORT",
      "CURSOR_API_KEY",
      "CLI_API_KEY",
      "EXECUTION_TIMEOUT_MS",
      "SESSION_ENABLED",
      "SESSION_DIR",
      "SESSION_RETENTION_DAYS",
      "AGENTS_SETTINGS_PATH"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "AGENTS_DIR",
      "envVar": "AGENTS_DIR",
      "label": "Absolute path to the directory containing agent definition files (",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Absolute path to the directory containing agent definition files (.md or .txt)"
    },
    {
      "id": "AGENT_TYPE",
      "envVar": "AGENT_TYPE",
      "label": "Type of AI CLI to use: 'cursor', 'claude', 'gemini', 'codex', 'command-code', 'glm', 'kimi', 'grok', 'antigravity', or 'opencode'",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Type of AI CLI to use: 'cursor', 'claude', 'gemini', 'codex', 'command-code', 'glm', 'kimi', 'grok', 'antigravity', or 'opencode'"
    },
    {
      "id": "AGENT_PERMISSION",
      "envVar": "AGENT_PERMISSION",
      "label": "Approval/sandbox level for sub-agents: 'read-only', 'safe-edit' (default), or 'yolo'",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Approval/sandbox level for sub-agents: 'read-only', 'safe-edit' (default), or 'yolo'"
    },
    {
      "id": "AGENT_MODEL",
      "envVar": "AGENT_MODEL",
      "label": "Optional model override applied to every sub-agent execution by this MCP server",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional model override applied to every sub-agent execution by this MCP server"
    },
    {
      "id": "AGENT_EFFORT",
      "envVar": "AGENT_EFFORT",
      "label": "Optional backend-specific reasoning effort or model variant",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional backend-specific reasoning effort or model variant. Supported by codex, claude, command-code, glm, kimi, grok, antigravity, and opencode."
    },
    {
      "id": "CURSOR_API_KEY",
      "envVar": "CURSOR_API_KEY",
      "label": "API key for cursor-agent authentication (used only when AGENT_TYPE=cursor; passed via env, never via CLI args)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "API key for cursor-agent authentication (used only when AGENT_TYPE=cursor; passed via env, never via CLI args)"
    },
    {
      "id": "CLI_API_KEY",
      "envVar": "CLI_API_KEY",
      "label": "Provider API key for AGENT_TYPE=glm or kimi (passed to the claude binary via env, never via CLI args)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Provider API key for AGENT_TYPE=glm or kimi (passed to the claude binary via env, never via CLI args). Restart/reconnect the MCP server after changing it."
    },
    {
      "id": "EXECUTION_TIMEOUT_MS",
      "envVar": "EXECUTION_TIMEOUT_MS",
      "label": "Agent execution timeout in milliseconds, as a positive whole number (default: 300000)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Agent execution timeout in milliseconds, as a positive whole number (default: 300000). The server refuses to start on an invalid value such as \"5min\"."
    },
    {
      "id": "SESSION_ENABLED",
      "envVar": "SESSION_ENABLED",
      "label": "Enable session management to maintain execution history across agent calls (default: false)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Enable session management to maintain execution history across agent calls (default: false)"
    },
    {
      "id": "SESSION_DIR",
      "envVar": "SESSION_DIR",
      "label": "Directory path for storing session files (default:",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory path for storing session files (default: .mcp-sessions in current working directory)"
    },
    {
      "id": "SESSION_RETENTION_DAYS",
      "envVar": "SESSION_RETENTION_DAYS",
      "label": "Number of days to retain session history before automatic cleanup (default: 1)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Number of days to retain session history before automatic cleanup (default: 1)"
    },
    {
      "id": "AGENTS_SETTINGS_PATH",
      "envVar": "AGENTS_SETTINGS_PATH",
      "label": "Path to CLI settings file/directory",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to CLI settings file/directory. Claude: --settings arg, Cursor: CURSOR_CONFIG_DIR, Codex: CODEX_HOME. GLM/Kimi/Gemini/Grok/Antigravity/OpenCode/Command Code not supported."
    }
  ]
}
