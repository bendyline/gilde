{
  "schemaVersion": 1,
  "version": "0.3.1",
  "releasedAt": "2026-06-05T20:32:47.667149Z",
  "runtime": {
    "kind": "npm-package",
    "package": "audit-ledger-mcp",
    "version": "0.3.1",
    "sha256": "12329107f646efc4d9c1bd2a55f5b367701aa046c8461295669b8887f2c45201",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "AUDIT_API_URL",
      "AUDIT_WRITE_KEY",
      "AUDIT_READ_KEY",
      "AUDIT_TIMEOUT_MS",
      "AUDIT_RETRY_ATTEMPTS",
      "AUDIT_HMAC_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "AUDIT_API_URL",
      "envVar": "AUDIT_API_URL",
      "label": "Your deployed audit-ledger API endpoint (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Your deployed audit-ledger API endpoint (e.g. https://<api-id>.execute-api.<region>.amazonaws.com/prod). Leave unset to run in sandbox mode against the public shared tenant."
    },
    {
      "id": "AUDIT_WRITE_KEY",
      "envVar": "AUDIT_WRITE_KEY",
      "label": "Tenant write key for your deployed ledger",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Tenant write key for your deployed ledger. Required if AUDIT_API_URL is set and you want to call record_decision."
    },
    {
      "id": "AUDIT_READ_KEY",
      "envVar": "AUDIT_READ_KEY",
      "label": "Tenant read key for your deployed ledger",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Tenant read key for your deployed ledger. Required if AUDIT_API_URL is set and you want to call verify_decision or list_decisions."
    },
    {
      "id": "AUDIT_TIMEOUT_MS",
      "envVar": "AUDIT_TIMEOUT_MS",
      "label": "HTTP timeout in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP timeout in milliseconds. Defaults to 5000."
    },
    {
      "id": "AUDIT_RETRY_ATTEMPTS",
      "envVar": "AUDIT_RETRY_ATTEMPTS",
      "label": "Number of retry attempts on transient failures",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Number of retry attempts on transient failures. Defaults to 3."
    },
    {
      "id": "AUDIT_HMAC_KEY",
      "envVar": "AUDIT_HMAC_KEY",
      "label": "Tenant secret used to HMAC-SHA256 PII and prompts locally before sending",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Tenant secret used to HMAC-SHA256 PII and prompts locally before sending. Generate once per tenant (e.g. node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\") and keep it in your environment. The key never leaves your process. If unset, the MCP falls back to plain SHA-256 for backwards compatibility and logs a one-time deprecation warning; set this to switch to keyed hashing, which regulators (ICO/EDPB) expect for pseudonymisation."
    }
  ]
}
