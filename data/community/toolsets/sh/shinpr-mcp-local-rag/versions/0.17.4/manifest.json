{
  "schemaVersion": 1,
  "version": "0.17.4",
  "releasedAt": "2026-08-12T03:14:37.588912Z",
  "runtime": {
    "kind": "npm-package",
    "package": "mcp-local-rag",
    "version": "0.17.4",
    "sha256": "41995a52b2e74b5d82a5d6f4783737200588ed3afa92e83fe35491a67ad90b95",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "BASE_DIR",
      "BASE_DIRS",
      "DB_PATH",
      "CACHE_DIR",
      "MODEL_NAME",
      "MAX_FILE_SIZE",
      "RAG_MAX_DISTANCE",
      "RAG_GROUPING",
      "RAG_MAX_FILES",
      "CHUNK_MIN_LENGTH",
      "RAG_DEVICE",
      "RAG_DTYPE",
      "RAG_HYBRID_WEIGHT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "BASE_DIR",
      "envVar": "BASE_DIR",
      "label": "Base directory for document storage (defaults to current working directory)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base directory for document storage (defaults to current working directory). Ignored when BASE_DIRS is set."
    },
    {
      "id": "BASE_DIRS",
      "envVar": "BASE_DIRS",
      "label": "JSON array of base directories (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "JSON array of base directories (e.g. '[\"/a\",\"/b\"]'). Takes precedence over BASE_DIR."
    },
    {
      "id": "DB_PATH",
      "envVar": "DB_PATH",
      "label": "Path to LanceDB database directory (defaults to",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to LanceDB database directory (defaults to ./lancedb/)"
    },
    {
      "id": "CACHE_DIR",
      "envVar": "CACHE_DIR",
      "label": "Directory where Transformers",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory where Transformers.js models are cached (defaults to ./models/)"
    },
    {
      "id": "MODEL_NAME",
      "envVar": "MODEL_NAME",
      "label": "Embedding model name (defaults to Xenova/all-MiniLM-L6-v2)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Embedding model name (defaults to Xenova/all-MiniLM-L6-v2)"
    },
    {
      "id": "MAX_FILE_SIZE",
      "envVar": "MAX_FILE_SIZE",
      "label": "Maximum file size in bytes (defaults to 104857600 / 100MB)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum file size in bytes (defaults to 104857600 / 100MB)"
    },
    {
      "id": "RAG_MAX_DISTANCE",
      "envVar": "RAG_MAX_DISTANCE",
      "label": "Maximum distance threshold for filtering search results",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum distance threshold for filtering search results. Results with distance greater than this value will be excluded. Lower values mean stricter filtering (e.g., 0.5 for high relevance only)"
    },
    {
      "id": "RAG_GROUPING",
      "envVar": "RAG_GROUPING",
      "label": "Grouping mode for quality filtering",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Grouping mode for quality filtering. 'similar' returns only the most similar group (stops at first distance jump). 'related' includes related groups (stops at second distance jump). Unset means no grouping filter"
    },
    {
      "id": "RAG_MAX_FILES",
      "envVar": "RAG_MAX_FILES",
      "label": "Maximum number of files to keep in search results",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum number of files to keep in search results. Results are filtered to include only chunks from the top N best-scoring files. For example, 1 returns only the single best-matching file's chunks. Unset means no file filtering."
    },
    {
      "id": "CHUNK_MIN_LENGTH",
      "envVar": "CHUNK_MIN_LENGTH",
      "label": "Minimum chunk length in characters (1-10000, defaults to 50)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Minimum chunk length in characters (1-10000, defaults to 50). Chunks shorter than this threshold are filtered out during ingestion."
    },
    {
      "id": "RAG_DEVICE",
      "envVar": "RAG_DEVICE",
      "label": "Execution device for the embedder (defaults to cpu)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Execution device for the embedder (defaults to cpu). Passed straight to ONNX Runtime; see the Transformers.js device source for the supported backend names. If the requested device fails to initialize, the server throws an error."
    },
    {
      "id": "RAG_DTYPE",
      "envVar": "RAG_DTYPE",
      "label": "Embedding quantization dtype for the embedder (defaults to fp32)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Embedding quantization dtype for the embedder (defaults to fp32). Opt-in and pass-through; accepts any dtype the chosen model provides (fp32, fp16, q8, int8, ...). If the model has no variant for the requested dtype, the server throws an error. Changing this changes the embedding space — re-ingest existing data."
    },
    {
      "id": "RAG_HYBRID_WEIGHT",
      "envVar": "RAG_HYBRID_WEIGHT",
      "label": "Keyword boost factor for hybrid search (0",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Keyword boost factor for hybrid search (0.0-1.0, defaults to 0.6). 0 means semantic similarity only; higher values increase the keyword-match contribution to the final score."
    }
  ]
}
