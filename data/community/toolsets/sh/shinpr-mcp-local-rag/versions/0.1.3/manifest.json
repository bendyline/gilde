{
  "schemaVersion": 1,
  "version": "0.1.3",
  "releasedAt": "2025-10-31T17:51:46.571565Z",
  "runtime": {
    "kind": "npm-package",
    "package": "mcp-local-rag",
    "version": "0.1.3",
    "sha256": "ac05e7280fb530293c8d91deed2b283d824f99f14d584daa767f18150201b7f4",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "BASE_DIR",
      "DB_PATH",
      "CACHE_DIR",
      "MODEL_NAME",
      "MAX_FILE_SIZE",
      "CHUNK_SIZE",
      "CHUNK_OVERLAP"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "BASE_DIR",
      "envVar": "BASE_DIR",
      "label": "Base directory for document storage (defaults to current working directory)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base directory for document storage (defaults to current working directory)"
    },
    {
      "id": "DB_PATH",
      "envVar": "DB_PATH",
      "label": "Path to LanceDB database directory (defaults to",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to LanceDB database directory (defaults to ./lancedb/)"
    },
    {
      "id": "CACHE_DIR",
      "envVar": "CACHE_DIR",
      "label": "Directory where Transformers",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory where Transformers.js models are cached (defaults to ./models/)"
    },
    {
      "id": "MODEL_NAME",
      "envVar": "MODEL_NAME",
      "label": "Embedding model name (defaults to Xenova/all-MiniLM-L6-v2)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Embedding model name (defaults to Xenova/all-MiniLM-L6-v2)"
    },
    {
      "id": "MAX_FILE_SIZE",
      "envVar": "MAX_FILE_SIZE",
      "label": "Maximum file size in bytes (defaults to 104857600 / 100MB)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum file size in bytes (defaults to 104857600 / 100MB)"
    },
    {
      "id": "CHUNK_SIZE",
      "envVar": "CHUNK_SIZE",
      "label": "Text chunk size for splitting documents (defaults to 512)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Text chunk size for splitting documents (defaults to 512)"
    },
    {
      "id": "CHUNK_OVERLAP",
      "envVar": "CHUNK_OVERLAP",
      "label": "Overlap between consecutive chunks (defaults to 100)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Overlap between consecutive chunks (defaults to 100)"
    }
  ]
}
