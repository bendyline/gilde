{
  "schemaVersion": 1,
  "version": "0.1.4",
  "releasedAt": "2026-06-07T09:24:26.290523Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@nexus-legal/mcp",
    "version": "0.1.4",
    "sha256": "e14e8b34eb8738de6a8bbdfc3985b42313671c39d55a4dc9e85ed118b43ffabe",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "NEXUS_API_KEY",
      "NEXUS_BASE_URL",
      "NEXUS_TIMEOUT_MS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "NEXUS_API_KEY",
      "envVar": "NEXUS_API_KEY",
      "label": "Personal API key (format `nlk_*`) with `mcp` scope",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Personal API key (format `nlk_*`) with `mcp` scope. Generate one at https://nexusquantum.legal/developers (select 'Servidor MCP' when creating the key)."
    },
    {
      "id": "NEXUS_BASE_URL",
      "envVar": "NEXUS_BASE_URL",
      "label": "Override base URL of the Nexus backend",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override base URL of the Nexus backend. Defaults to https://nexusquantum.legal. Change only for self-hosted or staging environments.",
      "default": "https://nexusquantum.legal"
    },
    {
      "id": "NEXUS_TIMEOUT_MS",
      "envVar": "NEXUS_TIMEOUT_MS",
      "label": "HTTP timeout in milliseconds for long-running analyses",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP timeout in milliseconds for long-running analyses. Default 180000 (3 minutes).",
      "default": "180000"
    }
  ]
}
