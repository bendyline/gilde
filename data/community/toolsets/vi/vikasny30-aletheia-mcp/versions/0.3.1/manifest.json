{
  "schemaVersion": 1,
  "version": "0.3.1",
  "releasedAt": "2026-09-13T14:09:41.255244Z",
  "runtime": {
    "kind": "npm-package",
    "package": "aletheia-mcp",
    "version": "0.3.1",
    "sha256": "4e4d6f0cbbce59f53885b219975c23b7bc7af4665c381c04039728fa0698a0a4",
    "entry": "dist/index.js",
    "args": [
      "--allow-write",
      "--allow-network",
      "--allow-loopback",
      "--allowed-paths"
    ],
    "envHints": [
      "ALETHEIA_ALLOW_WRITE",
      "ALETHEIA_ALLOW_NETWORK",
      "ALETHEIA_ALLOW_LOOPBACK",
      "ALETHEIA_OPERATOR_SECRET"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "ALETHEIA_ALLOW_WRITE",
      "envVar": "ALETHEIA_ALLOW_WRITE",
      "label": "Set to \"true\" to authorize filesystem writes (equivalent to --allow-write)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to authorize filesystem writes (equivalent to --allow-write)."
    },
    {
      "id": "ALETHEIA_ALLOW_NETWORK",
      "envVar": "ALETHEIA_ALLOW_NETWORK",
      "label": "Set to \"true\" to authorize outbound network requests (equivalent to --allow-network)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to authorize outbound network requests (equivalent to --allow-network)."
    },
    {
      "id": "ALETHEIA_ALLOW_LOOPBACK",
      "envVar": "ALETHEIA_ALLOW_LOOPBACK",
      "label": "Set to \"true\" to authorize requests to localhost / 127",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to authorize requests to localhost / 127.0.0.1 (equivalent to --allow-loopback)."
    },
    {
      "id": "ALETHEIA_OPERATOR_SECRET",
      "envVar": "ALETHEIA_OPERATOR_SECRET",
      "label": "Operator token required to loosen an already-running session's mandate at runtime",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Operator token required to loosen an already-running session's mandate at runtime."
    }
  ]
}
