{
  "schemaVersion": 1,
  "version": "0.3.2",
  "releasedAt": "2026-08-06T17:39:21.205879Z",
  "runtime": {
    "kind": "npm-package",
    "package": "hilan-mcp",
    "version": "0.3.2",
    "sha256": "1886097bd812690af5bcf26e8d4b6e62852f129f581d23d0d64d7ea8985f7d54",
    "entry": "dist/cli/index.js",
    "args": [],
    "envHints": [
      "COMPANY_URL",
      "COMPANY_TENANT",
      "COMPANY_CODE",
      "HILAN_DB_KEY",
      "NODE_EXTRA_CA_CERTS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "COMPANY_URL",
      "envVar": "COMPANY_URL",
      "label": "Default tenant as a full URL, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default tenant as a full URL, e.g. \"https://amdocs.net.hilan.co.il\" (optional if you always pass a `tenant` argument to tools instead). Takes priority over COMPANY_TENANT/COMPANY_CODE."
    },
    {
      "id": "COMPANY_TENANT",
      "envVar": "COMPANY_TENANT",
      "label": "Default tenant subdomain, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default tenant subdomain, e.g. \"amdocs\" (optional if COMPANY_URL is set or a `tenant` argument is always passed to tools)."
    },
    {
      "id": "COMPANY_CODE",
      "envVar": "COMPANY_CODE",
      "label": "Default tenant as a numeric org code, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default tenant as a numeric org code, e.g. \"5227\" (optional, lowest priority of the three tenant env vars)."
    },
    {
      "id": "HILAN_DB_KEY",
      "envVar": "HILAN_DB_KEY",
      "label": "Database encryption key (only needed if you skip `npx hilan-mcp setup-key`, which stores it in your OS credential store instead)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Database encryption key (only needed if you skip `npx hilan-mcp setup-key`, which stores it in your OS credential store instead)."
    },
    {
      "id": "NODE_EXTRA_CA_CERTS",
      "envVar": "NODE_EXTRA_CA_CERTS",
      "label": "Path to a CA bundle, only needed on a network with a TLS-intercepting corporate proxy (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to a CA bundle, only needed on a network with a TLS-intercepting corporate proxy (e.g. \"self-signed certificate in certificate chain\" errors)."
    }
  ]
}
