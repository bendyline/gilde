{
  "schemaVersion": 1,
  "version": "0.2.0",
  "releasedAt": "2026-08-06T19:44:02.43911Z",
  "runtime": {
    "kind": "npm-package",
    "package": "ghost-inspector-mcp",
    "version": "0.2.0",
    "sha256": "8039bb47ec4cc9cfb40670cb816d734f06dbe890457709fb76dcf35f880b36ce",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "GHOST_INSPECTOR_API_KEY",
      "GHOST_INSPECTOR_ORG_ID",
      "GHOST_INSPECTOR_ALLOW_WRITES",
      "GHOST_INSPECTOR_ALLOW_RUNS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "GHOST_INSPECTOR_API_KEY",
      "envVar": "GHOST_INSPECTOR_API_KEY",
      "label": "Your personal Ghost Inspector key, from Account Settings then API Access",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Your personal Ghost Inspector key, from Account Settings then API Access. Read fresh on every call, never written to disk, never echoed back."
    },
    {
      "id": "GHOST_INSPECTOR_ORG_ID",
      "envVar": "GHOST_INSPECTOR_ORG_ID",
      "label": "Organization id",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Organization id. Needed only to execute a validation; read it from gi_whoami."
    },
    {
      "id": "GHOST_INSPECTOR_ALLOW_WRITES",
      "envVar": "GHOST_INSPECTOR_ALLOW_WRITES",
      "label": "Set to exactly true to register the two mutating tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to exactly true to register the two mutating tools. Anything else keeps the server read-only."
    },
    {
      "id": "GHOST_INSPECTOR_ALLOW_RUNS",
      "envVar": "GHOST_INSPECTOR_ALLOW_RUNS",
      "label": "Set to \"true\" to let gi_run_test execute a stored test against a real environment, which can submit a real form",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to let gi_run_test execute a stored test against a real environment, which can submit a real form. Deliberately not implied by GHOST_INSPECTOR_ALLOW_WRITES: an edit can be rolled back from the backup this server returns, a submission cannot. Defaults to false."
    }
  ]
}
