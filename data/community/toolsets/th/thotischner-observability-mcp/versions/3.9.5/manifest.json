{
  "schemaVersion": 1,
  "version": "3.9.5",
  "releasedAt": "2026-08-02T23:59:21.875349Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@thotischner/observability-mcp",
    "version": "3.9.5",
    "sha256": "03d3a62aa33104209febf7cda8871e47093c6c485566ab9fcf0c564cbc327f11",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "MCP_TRANSPORT",
      "PROMETHEUS_URL",
      "LOKI_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MCP_TRANSPORT",
      "envVar": "MCP_TRANSPORT",
      "label": "Set to 'stdio' when an MCP client spawns the package directly (the npx default starts the HTTP gateway + Web UI on :3000 instead)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'stdio' when an MCP client spawns the package directly (the npx default starts the HTTP gateway + Web UI on :3000 instead)."
    },
    {
      "id": "PROMETHEUS_URL",
      "envVar": "PROMETHEUS_URL",
      "label": "Prometheus base URL to connect as a metrics source (optional; sources are also configurable via the Web UI / sources",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Prometheus base URL to connect as a metrics source (optional; sources are also configurable via the Web UI / sources.yaml)."
    },
    {
      "id": "LOKI_URL",
      "envVar": "LOKI_URL",
      "label": "Loki base URL to connect as a logs source (optional)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Loki base URL to connect as a logs source (optional)."
    }
  ]
}
