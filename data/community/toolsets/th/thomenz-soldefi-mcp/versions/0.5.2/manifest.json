{
  "schemaVersion": 1,
  "version": "0.5.2",
  "releasedAt": "2026-08-04T17:25:28.483359Z",
  "runtime": {
    "kind": "npm-package",
    "package": "soldefi-mcp",
    "version": "0.5.2",
    "sha256": "033311dd96ac327eb2b7be5fef55627579a8cc79102a642fc026efca7e669b1d",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "SOLANA_PRIVATE_KEY",
      "EVM_PRIVATE_KEY",
      "SOLANA_RPC_URL",
      "SOLDEFI_BASE_URL",
      "X402_NETWORK"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SOLANA_PRIVATE_KEY",
      "envVar": "SOLANA_PRIVATE_KEY",
      "label": "base58 or JSON-array secret key of a DEDICATED Solana wallet holding USDC (pays per call)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "base58 or JSON-array secret key of a DEDICATED Solana wallet holding USDC (pays per call). Configure this and/or EVM_PRIVATE_KEY; the x402 layer settles on whichever rail the 402 advertises. Paid tools return 402 without one; the free validate_mint tool still works."
    },
    {
      "id": "EVM_PRIVATE_KEY",
      "envVar": "EVM_PRIVATE_KEY",
      "label": "0x private key of a DEDICATED wallet holding USDC on Base (pays per call)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "0x private key of a DEDICATED wallet holding USDC on Base (pays per call). Configure this and/or SOLANA_PRIVATE_KEY; the x402 layer settles on whichever rail the 402 advertises. Paid tools return 402 without one; the free validate_mint tool still works."
    },
    {
      "id": "SOLANA_RPC_URL",
      "envVar": "SOLANA_RPC_URL",
      "label": "Optional Solana RPC override used to build the payment (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional Solana RPC override used to build the payment (e.g. a Helius URL). Only read when SOLANA_PRIVATE_KEY is set."
    },
    {
      "id": "SOLDEFI_BASE_URL",
      "envVar": "SOLDEFI_BASE_URL",
      "label": "Base URL of a soldefi Worker",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of a soldefi Worker. Defaults to the hosted production API (https://soldefi.thomenz.me)."
    },
    {
      "id": "X402_NETWORK",
      "envVar": "X402_NETWORK",
      "label": "Set to 'base-sepolia' to pay on testnet, which switches BOTH rails (Solana devnet + Base Sepolia)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'base-sepolia' to pay on testnet, which switches BOTH rails (Solana devnet + Base Sepolia). Any other value, including unset, pays on mainnet."
    }
  ]
}
