{
  "schemaVersion": 1,
  "version": "0.1.3",
  "releasedAt": "2026-09-05T02:23:08.169884Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@the-rfp-hub/mcp",
    "version": "0.1.3",
    "sha256": "8466647ebc1151edae84fcec1014642e07ae967cc7f64a8885ad37b79a8fff7d",
    "entry": "dist/cli.js",
    "args": [
      "--state-dir"
    ],
    "envHints": [
      "RFPHUB_API_BASE",
      "RFPHUB_API_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "RFPHUB_API_BASE",
      "envVar": "RFPHUB_API_BASE",
      "label": "Base URL of the RFP Hub API",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the RFP Hub API. Defaults to https://api.ethrfps.app. The origin of this URL is bound into every write approval."
    },
    {
      "id": "RFPHUB_API_KEY",
      "envVar": "RFPHUB_API_KEY",
      "label": "RFP Hub API credential",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "RFP Hub API credential. Needed ONLY to submit; searching and fetching are anonymous and never send it. Setting it is what registers the write tool at all: without it tools/list returns two tools. The key must carry the write scope and must not carry publish; the server checks that before its first submission, so a submission waits for a reviewer's decision."
    }
  ]
}
