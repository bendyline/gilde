{
  "schemaVersion": 1,
  "version": "1.10.1",
  "releasedAt": "2026-07-25T03:27:56.312743Z",
  "runtime": {
    "kind": "npm-package",
    "package": "ncloud-mcp-server",
    "version": "1.10.1",
    "sha256": "025dd19c7cafbe022ab1fc6e27f47e4923bacee8b0e98da1205b09fbab1c47be",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "NCLOUD_ACCESS_KEY",
      "NCLOUD_SECRET_KEY",
      "NCLOUD_REGION",
      "NCLOUD_TOOL_GROUPS",
      "NCLOUD_RESPONSE_PRUNE",
      "NCLOUD_TIMEOUT_MS",
      "NCLOUD_LANG",
      "NCLOUD_RESPONSE_MAXBYTES"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "NCLOUD_ACCESS_KEY",
      "envVar": "NCLOUD_ACCESS_KEY",
      "label": "Ncloud API Access Key",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Ncloud API Access Key"
    },
    {
      "id": "NCLOUD_SECRET_KEY",
      "envVar": "NCLOUD_SECRET_KEY",
      "label": "Ncloud API Secret Key",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Ncloud API Secret Key"
    },
    {
      "id": "NCLOUD_REGION",
      "envVar": "NCLOUD_REGION",
      "label": "Ncloud region code (KR, JPN, SGN, USWN, DEN)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Ncloud region code (KR, JPN, SGN, USWN, DEN)"
    },
    {
      "id": "NCLOUD_TOOL_GROUPS",
      "envVar": "NCLOUD_TOOL_GROUPS",
      "label": "Comma-separated tool groups to load at startup, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated tool groups to load at startup, e.g. 'compute,network,billing' or 'all,-billing'. Unset = all groups. Include the 'dynamic' keyword to start with the core set (common+compute+network+database) AND allow mid-session expansion via the ncloud_enable_tool_group meta tool; without 'dynamic', the listed groups are locked (no runtime expansion). A '-'-excluded group is also refused for dynamic enable. common is always on."
    },
    {
      "id": "NCLOUD_RESPONSE_PRUNE",
      "envVar": "NCLOUD_RESPONSE_PRUNE",
      "label": "Set to '1' to strip empty values (null/\"\"/[]/{}) from responses globally",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to '1' to strip empty values (null/\"\"/[]/{}) from responses globally."
    },
    {
      "id": "NCLOUD_TIMEOUT_MS",
      "envVar": "NCLOUD_TIMEOUT_MS",
      "label": "API request timeout in milliseconds (default 30000)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "API request timeout in milliseconds (default 30000). HTTP 429 is always auto-retried up to 2 times; read-only query tools also retry 503/504 and network errors."
    },
    {
      "id": "NCLOUD_LANG",
      "envVar": "NCLOUD_LANG",
      "label": "Language for client error messages: 'en' for English, otherwise/unset is Korean (default)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Language for client error messages: 'en' for English, otherwise/unset is Korean (default)."
    },
    {
      "id": "NCLOUD_RESPONSE_MAXBYTES",
      "envVar": "NCLOUD_RESPONSE_MAXBYTES",
      "label": "Opt-in response-size guard for read-only tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Opt-in response-size guard for read-only tools. When set to a positive byte threshold, large read-only responses are truncated item-by-item to stay under it, adding 'truncated'/'suggestedPageSize' recovery hints. Unset/0 = guard off (response shape unchanged)."
    }
  ]
}
