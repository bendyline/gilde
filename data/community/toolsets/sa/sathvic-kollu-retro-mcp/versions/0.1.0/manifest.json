{
  "schemaVersion": 1,
  "version": "0.1.0",
  "releasedAt": "2026-06-17T13:40:07.985433Z",
  "runtime": {
    "kind": "npm-package",
    "package": "retro-mcp",
    "version": "0.1.0",
    "sha256": "2432c2f7d0621e3b2256b6fb29e300d45b4452d721a5541507387c272e71b37b",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "JIRA_BASE_URL",
      "JIRA_EMAIL",
      "JIRA_API_TOKEN",
      "JIRA_BOARD_ID",
      "GITHUB_TOKEN",
      "GITHUB_REPOS",
      "SLACK_TOKEN",
      "SLACK_CHANNELS",
      "RETRO_DEMO_MODE"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "JIRA_BASE_URL",
      "envVar": "JIRA_BASE_URL",
      "label": "Your Jira Cloud site, e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Your Jira Cloud site, e.g. https://your-company.atlassian.net. Required for a live retro."
    },
    {
      "id": "JIRA_EMAIL",
      "envVar": "JIRA_EMAIL",
      "label": "Atlassian account email the API token belongs to",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Atlassian account email the API token belongs to."
    },
    {
      "id": "JIRA_API_TOKEN",
      "envVar": "JIRA_API_TOKEN",
      "label": "Jira API token from id",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Jira API token from id.atlassian.com. Read-only use."
    },
    {
      "id": "JIRA_BOARD_ID",
      "envVar": "JIRA_BOARD_ID",
      "label": "Optional board id to pin when the account has several boards",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional board id to pin when the account has several boards."
    },
    {
      "id": "GITHUB_TOKEN",
      "envVar": "GITHUB_TOKEN",
      "label": "Optional GitHub read-only token for PR review latency and reverts",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional GitHub read-only token for PR review latency and reverts."
    },
    {
      "id": "GITHUB_REPOS",
      "envVar": "GITHUB_REPOS",
      "label": "Optional comma-separated owner/name repos to scope PR analysis",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional comma-separated owner/name repos to scope PR analysis."
    },
    {
      "id": "SLACK_TOKEN",
      "envVar": "SLACK_TOKEN",
      "label": "Optional Slack read token to scan sprint channels for blocker and incident language",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional Slack read token to scan sprint channels for blocker and incident language."
    },
    {
      "id": "SLACK_CHANNELS",
      "envVar": "SLACK_CHANNELS",
      "label": "Optional comma-separated Slack channel ids to scan for the sprint window",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional comma-separated Slack channel ids to scan for the sprint window."
    },
    {
      "id": "RETRO_DEMO_MODE",
      "envVar": "RETRO_DEMO_MODE",
      "label": "Set to true to force the synthetic demo sprint even when credentials are present",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to force the synthetic demo sprint even when credentials are present."
    }
  ]
}
