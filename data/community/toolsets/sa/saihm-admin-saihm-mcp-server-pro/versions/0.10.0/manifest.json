{
  "schemaVersion": 1,
  "version": "0.10.0",
  "releasedAt": "2026-09-14T21:48:57.927483Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@saihm/mcp-server-pro",
    "version": "0.10.0",
    "sha256": "320bb5bdc9fc21822497301f919dc7be79a4c65886345c212e91ec446351c68d",
    "entry": "dist/server.js",
    "args": [],
    "envHints": [
      "SAIHM_ENDPOINT_URL",
      "SAIHM_SELF_JOIN",
      "SAIHM_MASTER_SECRET_FILE",
      "SAIHM_HOME"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SAIHM_ENDPOINT_URL",
      "envVar": "SAIHM_ENDPOINT_URL",
      "label": "SAIHM operator endpoint",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "SAIHM operator endpoint. Optional: defaults to the hosted service at https://saihm.coti.global/mcp.",
      "default": "https://saihm.coti.global/mcp"
    },
    {
      "id": "SAIHM_SELF_JOIN",
      "envVar": "SAIHM_SELF_JOIN",
      "label": "Self-join is ON by default: the saihm_join tool generates a non-custodial identity on this device and activates the free trial, so no master secret is needed",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Self-join is ON by default: the saihm_join tool generates a non-custodial identity on this device and activates the free trial, so no master secret is needed. Set to '0' to suppress the tool."
    },
    {
      "id": "SAIHM_MASTER_SECRET_FILE",
      "envVar": "SAIHM_MASTER_SECRET_FILE",
      "label": "Path to a mode-600 file holding a 64+ hex-char master secret",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Path to a mode-600 file holding a 64+ hex-char master secret. Only needed if you opt out of self-join or are restoring an existing identity. Never transmitted."
    },
    {
      "id": "SAIHM_HOME",
      "envVar": "SAIHM_HOME",
      "label": "Base dir for the self-join identity file ($SAIHM_HOME/free-identity",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base dir for the self-join identity file ($SAIHM_HOME/free-identity.key, mode 600). Defaults to ~/.saihm. Also the fallback state directory when SAIHM_STATE_DIR is unset."
    }
  ]
}
