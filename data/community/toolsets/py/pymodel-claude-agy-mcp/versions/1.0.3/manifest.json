{
  "schemaVersion": 1,
  "version": "1.0.3",
  "releasedAt": "2026-08-14T03:01:38.619768Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@pymodel/claude-agy-mcp",
    "version": "1.0.3",
    "sha256": "16e4f0193eb1bba1f90c944abc4a41d293cde70d9faef6d6bbd1b6cbecaa1f56",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "AGY_PATH",
      "AGY_TIMEOUT",
      "AGY_MAX_RUNTIME",
      "AGY_MAX_OUTPUT_CHARS",
      "AGY_DEFAULT_MODEL",
      "AGY_ON_FAILURE"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "AGY_PATH",
      "envVar": "AGY_PATH",
      "label": "Path to the agy binary (defaults to `agy` on PATH)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to the agy binary (defaults to `agy` on PATH)"
    },
    {
      "id": "AGY_TIMEOUT",
      "envVar": "AGY_TIMEOUT",
      "label": "Override the runtime ceiling for all tools, in seconds (use AGY_TIMEOUT_<TOOL> to override a single tool)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the runtime ceiling for all tools, in seconds (use AGY_TIMEOUT_<TOOL> to override a single tool)"
    },
    {
      "id": "AGY_MAX_RUNTIME",
      "envVar": "AGY_MAX_RUNTIME",
      "label": "Absolute runtime ceiling in seconds (default 3600); the bridge does not kill on inactivity, only on cancellation, quota exhaustion, or this ceiling",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Absolute runtime ceiling in seconds (default 3600); the bridge does not kill on inactivity, only on cancellation, quota exhaustion, or this ceiling"
    },
    {
      "id": "AGY_MAX_OUTPUT_CHARS",
      "envVar": "AGY_MAX_OUTPUT_CHARS",
      "label": "Truncation cap for tool output, in characters (default 50000)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Truncation cap for tool output, in characters (default 50000)"
    },
    {
      "id": "AGY_DEFAULT_MODEL",
      "envVar": "AGY_DEFAULT_MODEL",
      "label": "Fallback model when no chain entry is available (defaults to Gemini 3",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Fallback model when no chain entry is available (defaults to Gemini 3.6 Flash (High))"
    },
    {
      "id": "AGY_ON_FAILURE",
      "envVar": "AGY_ON_FAILURE",
      "label": "Set to `strict` to instruct the calling agent not to absorb failed delegations (default `fallback`)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to `strict` to instruct the calling agent not to absorb failed delegations (default `fallback`)"
    }
  ]
}
