{
  "schemaVersion": 1,
  "version": "0.3.1",
  "releasedAt": "2026-07-27T18:04:40.660868Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@lastehr/mcp",
    "version": "0.3.1",
    "sha256": "c0ea47755e4eb628d25786b6e636dde14afe851b5c1070a7a14171c314f56891",
    "entry": "dist/cli.js",
    "args": [],
    "envHints": [
      "MEDPLUM_ACCESS_TOKEN",
      "MEDPLUM_CLIENT_ID",
      "MEDPLUM_CLIENT_SECRET",
      "MEDPLUM_BASE_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MEDPLUM_ACCESS_TOKEN",
      "envVar": "MEDPLUM_ACCESS_TOKEN",
      "label": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)."
    },
    {
      "id": "MEDPLUM_CLIENT_ID",
      "envVar": "MEDPLUM_CLIENT_ID",
      "label": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)."
    },
    {
      "id": "MEDPLUM_CLIENT_SECRET",
      "envVar": "MEDPLUM_CLIENT_SECRET",
      "label": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)."
    },
    {
      "id": "MEDPLUM_BASE_URL",
      "envVar": "MEDPLUM_BASE_URL",
      "label": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "FHIR chart tools over Medplum or a local HAPI stack: read-only by default, with an opt-in elicitation-gated write profile (human-approved per action)."
    }
  ]
}
