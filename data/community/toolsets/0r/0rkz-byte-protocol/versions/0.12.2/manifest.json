{
  "schemaVersion": 1,
  "version": "0.12.2",
  "releasedAt": "2026-08-20T01:22:45.53374Z",
  "runtime": {
    "kind": "npm-package",
    "package": "byte-mcp-server",
    "version": "0.12.2",
    "sha256": "9835b5e3d9a0caa0996b362382a858d65ba6f72d99f5e3e8c1f9890419255ecb",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "PRIVATE_KEY",
      "RPC_URL",
      "BYTE_GATEWAY_ATTESTER"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "PRIVATE_KEY",
      "envVar": "PRIVATE_KEY",
      "label": "Ethereum private key for write/buy tools (subscribe, unsubscribe, publish, buy_data)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Ethereum private key for write/buy tools (subscribe, unsubscribe, publish, buy_data). buy_data spends real Base-mainnet USDC — use a dedicated wallet. Not required for read-only tools."
    },
    {
      "id": "RPC_URL",
      "envVar": "RPC_URL",
      "label": "RPC for the on-chain library + EIP-712 attestation layer (Arbitrum Sepolia, eip155:421614)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "RPC for the on-chain library + EIP-712 attestation layer (Arbitrum Sepolia, eip155:421614). The x402 buy rail settles USDC on Base mainnet and needs no RPC here. Defaults to Arbitrum Sepolia public RPC."
    },
    {
      "id": "BYTE_GATEWAY_ATTESTER",
      "envVar": "BYTE_GATEWAY_ATTESTER",
      "label": "Gateway attester address byte_buy_data pins receipts against",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Gateway attester address byte_buy_data pins receipts against. Defaults to the current PayPerByte gateway attester (rotated 2026-08-19; previous default 0x77c86a...C472 is retired). Override only if the gateway discloses a rotation you need ahead of a package update - see /.well-known/agent.json receipt.attester and receipt.retiredAttesters."
    }
  ]
}
