{
  "schemaVersion": 1,
  "version": "0.4.1",
  "releasedAt": "2026-08-21T03:04:09.894911Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@sequesign/mcp",
    "version": "0.4.1",
    "sha256": "6e10220e5ce53f2279a8aa29409c4d3465f8269993899f98dda93a4fdfe2b3c2",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "SEQUESIGN_MODE",
      "SEQUESIGN_API_KEY",
      "SEQUESIGN_AGENT_PRIVATE_KEY",
      "SEQUESIGN_PACKAGE_DIR",
      "SEQUESIGN_TEMPLATE_REGISTRY_URL",
      "SEQUESIGN_TEMPLATE_REGISTRY_TOKEN"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SEQUESIGN_MODE",
      "envVar": "SEQUESIGN_MODE",
      "label": "Transport: 'direct' (default; sign locally, an independent witness co-signs) or 'managed' (route through the Sequesign broker for a registered identity)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Transport: 'direct' (default; sign locally, an independent witness co-signs) or 'managed' (route through the Sequesign broker for a registered identity)."
    },
    {
      "id": "SEQUESIGN_API_KEY",
      "envVar": "SEQUESIGN_API_KEY",
      "label": "Sequesign write-class API key (from the dashboard)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Sequesign write-class API key (from the dashboard). Required for managed mode; in direct mode it authenticates the hosted witness."
    },
    {
      "id": "SEQUESIGN_AGENT_PRIVATE_KEY",
      "envVar": "SEQUESIGN_AGENT_PRIVATE_KEY",
      "label": "Ed25519 PKCS#8 private-key PEM for the agent",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Ed25519 PKCS#8 private-key PEM for the agent. In managed mode, the key registered to your API key. In direct mode, omit to mint an ephemeral key per session."
    },
    {
      "id": "SEQUESIGN_PACKAGE_DIR",
      "envVar": "SEQUESIGN_PACKAGE_DIR",
      "label": "Directory where sealed receipt packages are written",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory where sealed receipt packages are written. Defaults to a temporary directory."
    },
    {
      "id": "SEQUESIGN_TEMPLATE_REGISTRY_URL",
      "envVar": "SEQUESIGN_TEMPLATE_REGISTRY_URL",
      "label": "Base URL of a read-only dynamic template registry (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of a read-only dynamic template registry (e.g. https://dashboard-api.sequesign.com/v1/registry). When set, direct-mode sessions and the discovery tools resolve templates published there in addition to the bundled ones, falling back to bundled offline. Omit to use only the bundled templates."
    },
    {
      "id": "SEQUESIGN_TEMPLATE_REGISTRY_TOKEN",
      "envVar": "SEQUESIGN_TEMPLATE_REGISTRY_TOKEN",
      "label": "Read-only bearer token for an account-scoped template registry (discovery only; never used in verification)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Read-only bearer token for an account-scoped template registry (discovery only; never used in verification). Omit for a public registry."
    }
  ]
}
