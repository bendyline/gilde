{
  "schemaVersion": 1,
  "version": "0.3.15",
  "releasedAt": "2026-07-29T22:18:50.941143Z",
  "runtime": {
    "kind": "npm-package",
    "package": "weavatrix-js",
    "version": "0.3.15",
    "sha256": "11c166b8a6b106db6311bfee8dd7476ca4b1d049e0ee2aa4b0181951d9756810",
    "entry": "bin/weavatrix-mcp.mjs",
    "args": [
      "offline"
    ],
    "envHints": [
      "WEAVATRIX_PRECISION",
      "WEAVATRIX_PRECISION_PREWARM",
      "WEAVATRIX_ALLOW_TEST_RUNS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "WEAVATRIX_PRECISION",
      "envVar": "WEAVATRIX_PRECISION",
      "label": "Optional semantic precision mode: lsp (default) uses the bundled bounded TypeScript/JavaScript language-server overlay; off keeps parser-only graphs",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional semantic precision mode: lsp (default) uses the bundled bounded TypeScript/JavaScript language-server overlay; off keeps parser-only graphs"
    },
    {
      "id": "WEAVATRIX_PRECISION_PREWARM",
      "envVar": "WEAVATRIX_PRECISION_PREWARM",
      "label": "Optional semantic prewarm budget: bounded (default) keeps fast startup; full explicitly queries every eligible JS/TS target within hard resource ceilings",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional semantic prewarm budget: bounded (default) keeps fast startup; full explicitly queries every eligible JS/TS target within hard resource ceilings"
    },
    {
      "id": "WEAVATRIX_ALLOW_TEST_RUNS",
      "envVar": "WEAVATRIX_ALLOW_TEST_RUNS",
      "label": "Optional explicit permission for verified_change to run requested test/check/verify package",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional explicit permission for verified_change to run requested test/check/verify package.json scripts; must be exactly 1"
    }
  ]
}
