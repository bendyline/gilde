{
  "schemaVersion": 1,
  "version": "1.0.0",
  "releasedAt": "2026-08-17T10:44:33.445355Z",
  "runtime": {
    "kind": "npm-package",
    "package": "weeek-mcp",
    "version": "1.0.0",
    "sha256": "84fdffd54f2b01959fdd861a0e23cda62df12f378ffca91f2a9b85cb3a347bcb",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "WEEEK_ACCESS_TOKEN",
      "READ_ONLY",
      "ENABLED_TOOLS",
      "WEEEK_BASE_URL",
      "WEEEK_TIMEOUT_MS",
      "MAX_RESPONSE_CHARS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "WEEEK_ACCESS_TOKEN",
      "envVar": "WEEEK_ACCESS_TOKEN",
      "label": "Weeek Public API v1 access token",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Weeek Public API v1 access token. Required — the server refuses to start without it."
    },
    {
      "id": "READ_ONLY",
      "envVar": "READ_ONLY",
      "label": "Gate for the write tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Gate for the write tools. Defaults to 'true': a fresh install exposes only the ten read tools. Set to 'false' to additionally expose the five write tools (create/update/move/complete a task, set an MR link) — an explicit operator opt-in, never the default.",
      "default": "true"
    },
    {
      "id": "ENABLED_TOOLS",
      "envVar": "ENABLED_TOOLS",
      "label": "Optional comma-separated allowlist of tool names to register (still subject to READ_ONLY)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional comma-separated allowlist of tool names to register (still subject to READ_ONLY). Pair with READ_ONLY=false for a granular write rollout, e.g. 'weeek_complete_task'."
    },
    {
      "id": "WEEEK_BASE_URL",
      "envVar": "WEEEK_BASE_URL",
      "label": "Override the Weeek API base URL",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the Weeek API base URL. Defaults to the public v1 endpoint.",
      "default": "https://api.weeek.net/public/v1"
    },
    {
      "id": "WEEEK_TIMEOUT_MS",
      "envVar": "WEEEK_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds.",
      "default": "30000"
    },
    {
      "id": "MAX_RESPONSE_CHARS",
      "envVar": "MAX_RESPONSE_CHARS",
      "label": "Upper bound on the JSON length of a single tool response, protecting the agent's context window from unbounded list/detail payloads",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Upper bound on the JSON length of a single tool response, protecting the agent's context window from unbounded list/detail payloads.",
      "default": "65536"
    }
  ]
}
