{
  "schemaVersion": 1,
  "version": "0.1.0",
  "releasedAt": "2026-09-01T21:03:45.788571Z",
  "runtime": {
    "kind": "npm-package",
    "package": "kleinanzeigen-mcp",
    "version": "0.1.0",
    "sha256": "8cf59b94f87a2ade158dccdc00a250924e36aff1c59491731611ab2bf8baa1d0",
    "entry": "dist/kleinanzeigen/index.js",
    "args": [],
    "envHints": [
      "WILLMEHR_MODE",
      "KZ_MIN_INTERVAL_MS",
      "KZ_MAX_PAGE_COUNT",
      "KZ_MAX_BATCH_SIZE",
      "TZ"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "WILLMEHR_MODE",
      "envVar": "WILLMEHR_MODE",
      "label": "'full' (default) for a local install; 'public' paces requests and lowers the page and batch caps for an instance shared by several people",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "'full' (default) for a local install; 'public' paces requests and lowers the page and batch caps for an instance shared by several people.",
      "default": "full",
      "options": [
        "full",
        "public"
      ]
    },
    {
      "id": "KZ_MIN_INTERVAL_MS",
      "envVar": "KZ_MIN_INTERVAL_MS",
      "label": "Minimum spacing between requests to kleinanzeigen",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Minimum spacing between requests to kleinanzeigen.de, in milliseconds. Concurrency trips the site's bot detection, so requests are serialised.",
      "default": "800"
    },
    {
      "id": "KZ_MAX_PAGE_COUNT",
      "envVar": "KZ_MAX_PAGE_COUNT",
      "label": "Cap on result pages a single search may fetch, roughly 25 listings each",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Cap on result pages a single search may fetch, roughly 25 listings each.",
      "default": "5"
    },
    {
      "id": "KZ_MAX_BATCH_SIZE",
      "envVar": "KZ_MAX_BATCH_SIZE",
      "label": "Cap on listing ids per batch detail call",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Cap on listing ids per batch detail call.",
      "default": "20"
    },
    {
      "id": "TZ",
      "envVar": "TZ",
      "label": "Listing timestamps are printed without a timezone; set Europe/Berlin to interpret them exactly",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Listing timestamps are printed without a timezone; set Europe/Berlin to interpret them exactly."
    }
  ]
}
