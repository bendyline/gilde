{
  "schemaVersion": 1,
  "version": "0.6.1",
  "releasedAt": "2026-07-27T18:03:02.249071Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@runcycles/mcp-server",
    "version": "0.6.1",
    "sha256": "bf22cdba6ebd1247180b35f4eaa3d14dee10de3a08f01a5dbfc9ff86159699b0",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "CYCLES_API_KEY",
      "CYCLES_BASE_URL",
      "CYCLES_MOCK",
      "CYCLES_DEFAULT_TENANT",
      "CYCLES_DEFAULT_WORKSPACE",
      "CYCLES_DEFAULT_APP",
      "CYCLES_DEFAULT_WORKFLOW",
      "CYCLES_DEFAULT_AGENT",
      "CYCLES_DEFAULT_TOOLSET"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "CYCLES_API_KEY",
      "envVar": "CYCLES_API_KEY",
      "label": "Cycles API key for authentication",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Cycles API key for authentication. Set CYCLES_MOCK=true for local development without a key."
    },
    {
      "id": "CYCLES_BASE_URL",
      "envVar": "CYCLES_BASE_URL",
      "label": "URL of the Cycles server (e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "URL of the Cycles server (e.g. https://api.runcycles.io). Required unless CYCLES_MOCK=true."
    },
    {
      "id": "CYCLES_MOCK",
      "envVar": "CYCLES_MOCK",
      "label": "Enable mock mode for local development — no API key or live server required",
      "type": "enum",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Enable mock mode for local development — no API key or live server required.",
      "default": "false",
      "options": [
        "true",
        "false"
      ]
    },
    {
      "id": "CYCLES_DEFAULT_TENANT",
      "envVar": "CYCLES_DEFAULT_TENANT",
      "label": "Optional default tenant merged into any tool call that omits it (explicit fields win)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional default tenant merged into any tool call that omits it (explicit fields win)."
    },
    {
      "id": "CYCLES_DEFAULT_WORKSPACE",
      "envVar": "CYCLES_DEFAULT_WORKSPACE",
      "label": "Optional default workspace merged into any tool call that omits it (explicit fields win)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional default workspace merged into any tool call that omits it (explicit fields win)."
    },
    {
      "id": "CYCLES_DEFAULT_APP",
      "envVar": "CYCLES_DEFAULT_APP",
      "label": "Optional default app merged into any tool call that omits it (explicit fields win)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional default app merged into any tool call that omits it (explicit fields win)."
    },
    {
      "id": "CYCLES_DEFAULT_WORKFLOW",
      "envVar": "CYCLES_DEFAULT_WORKFLOW",
      "label": "Optional default workflow merged into any tool call that omits it (explicit fields win)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional default workflow merged into any tool call that omits it (explicit fields win)."
    },
    {
      "id": "CYCLES_DEFAULT_AGENT",
      "envVar": "CYCLES_DEFAULT_AGENT",
      "label": "Optional default agent merged into any tool call that omits it (explicit fields win)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional default agent merged into any tool call that omits it (explicit fields win)."
    },
    {
      "id": "CYCLES_DEFAULT_TOOLSET",
      "envVar": "CYCLES_DEFAULT_TOOLSET",
      "label": "Optional default toolset merged into any tool call that omits it (explicit fields win)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional default toolset merged into any tool call that omits it (explicit fields win)."
    }
  ]
}
