{
  "schemaVersion": 1,
  "version": "0.5.3",
  "releasedAt": "2026-07-03T11:20:42.081974Z",
  "runtime": {
    "kind": "npm-package",
    "package": "garmin-mcp-unofficial",
    "version": "0.5.3",
    "sha256": "521ab7671a82b82af07c7a0347dd28daf321f27e1d354e7b40dc4f627a517621",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "GARMIN_TOKEN_PATH",
      "GARMIN_PRIVACY_MODE",
      "GARMIN_CACHE",
      "GARMIN_CACHE_PATH",
      "GARMIN_DOMAIN",
      "GARMIN_NO_CACHE"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "GARMIN_TOKEN_PATH",
      "envVar": "GARMIN_TOKEN_PATH",
      "label": "Optional local path for Garmin Connect tokens",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional local path for Garmin Connect tokens. Defaults to ~/.garmin-mcp/garmin_tokens.json. Create it with garmin-mcp-server auth (self-contained Node login, no Python)."
    },
    {
      "id": "GARMIN_PRIVACY_MODE",
      "envVar": "GARMIN_PRIVACY_MODE",
      "label": "Optional payload mode: summary, structured, or raw",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional payload mode: summary, structured, or raw. Defaults to structured. raw means full supported Garmin Connect payloads, not unrestricted raw sensor telemetry."
    },
    {
      "id": "GARMIN_CACHE",
      "envVar": "GARMIN_CACHE",
      "label": "Optional SQLite cache toggle",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional SQLite cache toggle. Set to true or sqlite to enable."
    },
    {
      "id": "GARMIN_CACHE_PATH",
      "envVar": "GARMIN_CACHE_PATH",
      "label": "Optional local SQLite cache path",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional local SQLite cache path. Defaults to ~/.garmin-mcp/cache.sqlite."
    },
    {
      "id": "GARMIN_DOMAIN",
      "envVar": "GARMIN_DOMAIN",
      "label": "Optional Garmin domain override",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional Garmin domain override. Use garmin.cn only for China accounts; default is garmin.com."
    },
    {
      "id": "GARMIN_NO_CACHE",
      "envVar": "GARMIN_NO_CACHE",
      "label": "Set to true to bypass the in-memory HTTP response cache (60s TTL for GET only)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to bypass the in-memory HTTP response cache (60s TTL for GET only). POST/PUT/DELETE and 4xx/5xx responses are never cached regardless."
    }
  ]
}
