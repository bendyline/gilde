{
  "schemaVersion": 1,
  "version": "0.4.7",
  "releasedAt": "2026-07-03T12:02:33.483109Z",
  "runtime": {
    "kind": "npm-package",
    "package": "withings-mcp-unofficial",
    "version": "0.4.7",
    "sha256": "3030f904a80babfba03679f7669a16ef3002bf715225698a54edee33c181a006",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "WITHINGS_CLIENT_ID",
      "WITHINGS_CLIENT_SECRET",
      "WITHINGS_REDIRECT_URI",
      "WITHINGS_TOKEN_PATH",
      "WITHINGS_PRIVACY_MODE",
      "WITHINGS_CACHE",
      "WITHINGS_CACHE_PATH",
      "WITHINGS_NO_CACHE"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "WITHINGS_CLIENT_ID",
      "envVar": "WITHINGS_CLIENT_ID",
      "label": "Withings OAuth client ID",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Withings OAuth client ID. Optional when configured with withings-mcp-server setup."
    },
    {
      "id": "WITHINGS_CLIENT_SECRET",
      "envVar": "WITHINGS_CLIENT_SECRET",
      "label": "Withings OAuth client secret",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Withings OAuth client secret. Prefer withings-mcp-server setup so this secret is stored in ~/.withings-mcp/config.json instead of MCP client config."
    },
    {
      "id": "WITHINGS_REDIRECT_URI",
      "envVar": "WITHINGS_REDIRECT_URI",
      "label": "Redirect URI configured in the Withings Developer Dashboard",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Redirect URI configured in the Withings Developer Dashboard. Optional when configured with withings-mcp-server setup."
    },
    {
      "id": "WITHINGS_TOKEN_PATH",
      "envVar": "WITHINGS_TOKEN_PATH",
      "label": "Optional local path for OAuth tokens",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional local path for OAuth tokens. Defaults to ~/.withings-mcp/tokens.json."
    },
    {
      "id": "WITHINGS_PRIVACY_MODE",
      "envVar": "WITHINGS_PRIVACY_MODE",
      "label": "Optional payload mode: summary, structured, or raw",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional payload mode: summary, structured, or raw. Defaults to structured. raw means full Withings API payloads, not continuous 24/7 raw sensor telemetry."
    },
    {
      "id": "WITHINGS_CACHE",
      "envVar": "WITHINGS_CACHE",
      "label": "Optional SQLite cache toggle",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional SQLite cache toggle. Set to true or sqlite to enable."
    },
    {
      "id": "WITHINGS_CACHE_PATH",
      "envVar": "WITHINGS_CACHE_PATH",
      "label": "Optional local SQLite cache path",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional local SQLite cache path. Defaults to ~/.withings-mcp/cache.sqlite."
    },
    {
      "id": "WITHINGS_NO_CACHE",
      "envVar": "WITHINGS_NO_CACHE",
      "label": "Set to true to bypass the in-memory HTTP response cache (60s TTL for GET only)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to true to bypass the in-memory HTTP response cache (60s TTL for GET only). POST/PUT/DELETE and 4xx/5xx responses are never cached regardless. Note: Withings's wbsapi.withings.net is POST-only today, so this is currently a no-op."
    }
  ]
}
