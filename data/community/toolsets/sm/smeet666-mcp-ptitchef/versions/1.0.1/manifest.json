{
  "schemaVersion": 1,
  "version": "1.0.1",
  "releasedAt": "2026-08-29T22:25:38.389801Z",
  "runtime": {
    "kind": "npm-package",
    "package": "mcp-ptitchef",
    "version": "1.0.1",
    "sha256": "033d324e67a677dd2d6ea6edef118f0ec6e42556c33c53298f585f01bea3c2d1",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "PTC_USER_AGENT",
      "PTC_MIN_INTERVAL_MS",
      "PTC_TIMEOUT_MS",
      "PTC_MAX_RETRIES",
      "PTC_CACHE_TTL_MS",
      "PTC_CACHE_MAX_ENTRIES",
      "PTC_MAX_BODY_BYTES",
      "PTC_BUDGET_MS",
      "PTC_LOG_LEVEL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "PTC_USER_AGENT",
      "envVar": "PTC_USER_AGENT",
      "label": "Identify your own client",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Identify your own client. This project's identifier stays appended, so the site can always reach a human."
    },
    {
      "id": "PTC_MIN_INTERVAL_MS",
      "envVar": "PTC_MIN_INTERVAL_MS",
      "label": "Minimum milliseconds between requests",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Minimum milliseconds between requests. Default 1500, floor 1000, ceiling 60000."
    },
    {
      "id": "PTC_TIMEOUT_MS",
      "envVar": "PTC_TIMEOUT_MS",
      "label": "Deadline for one request, in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Deadline for one request, in milliseconds. Default 20000."
    },
    {
      "id": "PTC_MAX_RETRIES",
      "envVar": "PTC_MAX_RETRIES",
      "label": "Retries when the site is busy or asks for room",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Retries when the site is busy or asks for room. Default 3."
    },
    {
      "id": "PTC_CACHE_TTL_MS",
      "envVar": "PTC_CACHE_TTL_MS",
      "label": "In-memory cache lifetime, in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "In-memory cache lifetime, in milliseconds. Default 900000. Set 0 to turn it off."
    },
    {
      "id": "PTC_CACHE_MAX_ENTRIES",
      "envVar": "PTC_CACHE_MAX_ENTRIES",
      "label": "In-memory cache size",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "In-memory cache size. Default 200."
    },
    {
      "id": "PTC_MAX_BODY_BYTES",
      "envVar": "PTC_MAX_BODY_BYTES",
      "label": "The most one page may run to before the read is abandoned",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "The most one page may run to before the read is abandoned. Default 8000000."
    },
    {
      "id": "PTC_BUDGET_MS",
      "envVar": "PTC_BUDGET_MS",
      "label": "The whole of the time one read may take, retries included",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "The whole of the time one read may take, retries included. Default 60000."
    },
    {
      "id": "PTC_LOG_LEVEL",
      "envVar": "PTC_LOG_LEVEL",
      "label": "silent, error, info or debug",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "silent, error, info or debug. Default error. Logs go to stderr."
    }
  ]
}
