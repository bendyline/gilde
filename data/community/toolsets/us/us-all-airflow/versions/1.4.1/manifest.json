{
  "schemaVersion": 1,
  "version": "1.4.1",
  "releasedAt": "2026-06-19T03:59:18.748095Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@us-all/airflow-mcp",
    "version": "1.4.1",
    "sha256": "066798558014679734c56be482ada7f5df8a0fc3e6efb58e7b307a12c438ad2f",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "AIRFLOW_API_URL",
      "AIRFLOW_USERNAME",
      "AIRFLOW_PASSWORD",
      "AIRFLOW_ALLOW_WRITE",
      "AIRFLOW_TOOLS",
      "AIRFLOW_DISABLE"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "AIRFLOW_API_URL",
      "envVar": "AIRFLOW_API_URL",
      "label": "Airflow host base, e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Airflow host base, e.g. 'http://airflow.example.com:8080'. Server prepends /api/v2 internally; trailing /api/v1 or /api/v2 is stripped if supplied."
    },
    {
      "id": "AIRFLOW_USERNAME",
      "envVar": "AIRFLOW_USERNAME",
      "label": "Airflow username (used to mint a JWT via SimpleAuthManager /auth/token)",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Airflow username (used to mint a JWT via SimpleAuthManager /auth/token)."
    },
    {
      "id": "AIRFLOW_PASSWORD",
      "envVar": "AIRFLOW_PASSWORD",
      "label": "Airflow password (used to mint a JWT via SimpleAuthManager /auth/token)",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Airflow password (used to mint a JWT via SimpleAuthManager /auth/token)."
    },
    {
      "id": "AIRFLOW_ALLOW_WRITE",
      "envVar": "AIRFLOW_ALLOW_WRITE",
      "label": "Set to 'true' to enable trigger-dag and clear-task tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 'true' to enable trigger-dag and clear-task tools. Default read-only.",
      "default": "false"
    },
    {
      "id": "AIRFLOW_TOOLS",
      "envVar": "AIRFLOW_TOOLS",
      "label": "Comma-separated category allowlist (airflow,meta)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated category allowlist (airflow,meta)."
    },
    {
      "id": "AIRFLOW_DISABLE",
      "envVar": "AIRFLOW_DISABLE",
      "label": "Comma-separated category disablelist",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated category disablelist."
    }
  ]
}
