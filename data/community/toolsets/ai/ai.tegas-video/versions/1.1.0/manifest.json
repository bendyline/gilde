{
  "schemaVersion": 1,
  "version": "1.1.0",
  "releasedAt": "2026-09-15T15:08:25.653331Z",
  "runtime": {
    "kind": "http-mcp",
    "url": "https://api.tegas.ai/mcp",
    "transport": "streamable-http",
    "authHint": "bearer",
    "envHints": [
      "Authorization"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "Authorization",
      "envVar": "Authorization",
      "label": "Optional: a Tegas API key as a bearer token, for clients that do not use OAuth",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional: a Tegas API key as a bearer token, for clients that do not use OAuth. Leave it empty to sign in with OAuth in the browser instead. Keys are created in the Tegas cabinet under Settings -> API.",
      "default": "Bearer {api_key}"
    }
  ]
}
