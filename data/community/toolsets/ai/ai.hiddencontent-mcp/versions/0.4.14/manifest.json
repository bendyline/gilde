{
  "schemaVersion": 1,
  "version": "0.4.14",
  "releasedAt": "2026-09-04T05:25:13.355828Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@hiddencontent/mcp",
    "version": "0.4.14",
    "sha256": "d4942d77161d0443943bf4ac3c3b9c3b7d58f5e80d8ec6511d73c285d937149a",
    "entry": "dist/bin.js",
    "args": [],
    "envHints": [
      "HCS_API_TOKEN",
      "HCS_URL",
      "HCS_CAPABILITIES"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "HCS_API_TOKEN",
      "envVar": "HCS_API_TOKEN",
      "label": "API token for the HiddenContent service",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "API token for the HiddenContent service. Get one at https://hiddencontent.ai/start?ref=mcp-registry — the server refuses to start without it and says so."
    },
    {
      "id": "HCS_URL",
      "envVar": "HCS_URL",
      "label": "Base URL of the service",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the service. Defaults to the hosted API; set it only when pointing at a self-hosted instance."
    },
    {
      "id": "HCS_CAPABILITIES",
      "envVar": "HCS_CAPABILITIES",
      "label": "Comma-separated list of what your agent can actually do",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated list of what your agent can actually do. The only accepted values are `tool-use`, `network-egress` and `code-execution`. These turn on the checks for concealed text that instructs a model to call a tool, send data or run code — declaring more can raise severity and never lowers it, and declaring nothing leaves those checks inert. Defaults to `tool-use` in the bundle install."
    }
  ]
}
