{
  "schemaVersion": 1,
  "version": "1.1.2",
  "releasedAt": "2026-09-16T09:35:34.287329Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@cyanheads/pixoo-mcp-server",
    "version": "1.1.2",
    "sha256": "1f0a32aaa493b782b5188dced65907990603e8169227893e4646a38fcb4950a2",
    "entry": "dist/index.js",
    "args": [
      "run",
      "start:stdio"
    ],
    "envHints": [
      "PIXOO_IP",
      "PIXOO_SIZE",
      "PIXOO_OUTPUT_DIR",
      "PIXOO_PUSH_MIN_INTERVAL_MS",
      "MCP_LOG_LEVEL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "PIXOO_IP",
      "envVar": "PIXOO_IP",
      "label": "Device IP address on the local network (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Device IP address on the local network (e.g. 192.168.1.50). Required for device tools; discovery and pure-render (push: false) work without it."
    },
    {
      "id": "PIXOO_SIZE",
      "envVar": "PIXOO_SIZE",
      "label": "Display size in pixels: 16, 32, or 64",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Display size in pixels: 16, 32, or 64. Must match the physical device.",
      "default": "64"
    },
    {
      "id": "PIXOO_OUTPUT_DIR",
      "envVar": "PIXOO_OUTPUT_DIR",
      "label": "Directory for auto-saving preview PNG and GIF files",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory for auto-saving preview PNG and GIF files. When unset, previews are returned in-response only."
    },
    {
      "id": "PIXOO_PUSH_MIN_INTERVAL_MS",
      "envVar": "PIXOO_PUSH_MIN_INTERVAL_MS",
      "label": "Minimum interval between device pushes in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Minimum interval between device pushes in milliseconds. Prevents device freeze from rapid-fire commands.",
      "default": "1000"
    },
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Sets the minimum log level for output (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Sets the minimum log level for output (e.g., 'debug', 'info', 'warn').",
      "default": "info"
    }
  ]
}
