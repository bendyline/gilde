{
  "schemaVersion": 1,
  "version": "0.7.2",
  "releasedAt": "2026-08-07T00:25:08.369759Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@cyanheads/gbif-biodiversity-mcp-server",
    "version": "0.7.2",
    "sha256": "cddbaeafd030f2410bba70d4400218865412af1537817023d481e746b7f05101",
    "entry": "dist/index.js",
    "args": [
      "run",
      "start:stdio"
    ],
    "envHints": [
      "MCP_LOG_LEVEL",
      "GBIF_BASE_URL",
      "GBIF_REQUEST_TIMEOUT_MS",
      "GBIF_USER_AGENT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Sets the minimum log level for output (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Sets the minimum log level for output (e.g., 'debug', 'info', 'warn').",
      "default": "info"
    },
    {
      "id": "GBIF_BASE_URL",
      "envVar": "GBIF_BASE_URL",
      "label": "Base URL every GBIF API request is built from",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL every GBIF API request is built from. Override to route through a proxy or a mirror of the GBIF v1 API.",
      "default": "https://api.gbif.org/v1"
    },
    {
      "id": "GBIF_REQUEST_TIMEOUT_MS",
      "envVar": "GBIF_REQUEST_TIMEOUT_MS",
      "label": "Milliseconds a single GBIF API request may take before it is aborted",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Milliseconds a single GBIF API request may take before it is aborted. Aborted attempts fall to the service's retry policy.",
      "default": "10000"
    },
    {
      "id": "GBIF_USER_AGENT",
      "envVar": "GBIF_USER_AGENT",
      "label": "Sent as the User-Agent header on every GBIF API request",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Sent as the User-Agent header on every GBIF API request. GBIF asks integrators to identify themselves with a contact URL or email. Unset or blank sends the server name, version, and repository URL."
    }
  ]
}
