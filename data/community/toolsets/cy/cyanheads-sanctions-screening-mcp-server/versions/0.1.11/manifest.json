{
  "schemaVersion": 1,
  "version": "0.1.11",
  "releasedAt": "2026-08-25T08:23:02.171403Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@cyanheads/sanctions-screening-mcp-server",
    "version": "0.1.11",
    "sha256": "1058f17c15830e387180d3d33cfadf4e0887a84cbfa9f69625f4204ac8d99520",
    "entry": "dist/index.js",
    "args": [
      "run",
      "start:stdio"
    ],
    "envHints": [
      "MCP_LOG_LEVEL",
      "SANCTIONS_MIRROR_PATH",
      "SANCTIONS_FUZZY_MIN_SCORE",
      "SANCTIONS_FUZZY_MAX_RESULTS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Sets the minimum log level for output (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Sets the minimum log level for output (e.g., 'debug', 'info', 'warn').",
      "default": "info"
    },
    {
      "id": "SANCTIONS_MIRROR_PATH",
      "envVar": "SANCTIONS_MIRROR_PATH",
      "label": "Filesystem path for the local SQLite mirror",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Filesystem path for the local SQLite mirror. All sources are keyless; no API key is required. Populate the mirror out-of-band with `bun run mirror:init`.",
      "default": "./data/sanctions.db"
    },
    {
      "id": "SANCTIONS_FUZZY_MIN_SCORE",
      "envVar": "SANCTIONS_FUZZY_MIN_SCORE",
      "label": "Default Jaro-Winkler similarity floor (0-1) for fuzzy matches when the caller omits minScore",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default Jaro-Winkler similarity floor (0-1) for fuzzy matches when the caller omits minScore.",
      "default": "0.85"
    },
    {
      "id": "SANCTIONS_FUZZY_MAX_RESULTS",
      "envVar": "SANCTIONS_FUZZY_MAX_RESULTS",
      "label": "Hard cap on fuzzy candidates scored per query, to bound work on short queries",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Hard cap on fuzzy candidates scored per query, to bound work on short queries.",
      "default": "50"
    }
  ]
}
