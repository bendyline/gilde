{
  "schemaVersion": 1,
  "version": "0.3.0",
  "releasedAt": "2026-04-25T02:06:20.658235Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@cyanheads/mailchimp-mcp-server",
    "version": "0.3.0",
    "sha256": "7e4ebbc3766c3eb4e637b50ac094ff4945c805fbc6ce7c80f11df7e9f8bfe9f9",
    "entry": "dist/index.js",
    "args": ["run", "start:stdio"],
    "envHints": [
      "MAILCHIMP_API_KEY",
      "MAILCHIMP_BASE_URL",
      "MAILCHIMP_TIMEOUT_MS",
      "MAILCHIMP_MAX_RETRIES",
      "MAILCHIMP_CONCURRENCY_LIMIT",
      "MAILCHIMP_ASSETS_DIR",
      "MAILCHIMP_TEMPLATES_DIR",
      "MCP_LOG_LEVEL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MAILCHIMP_API_KEY",
      "envVar": "MAILCHIMP_API_KEY",
      "label": "Mailchimp Marketing API key, including the '-dc' data-center suffix (e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Mailchimp Marketing API key, including the '-dc' data-center suffix (e.g. 'abc...-us22'). Generate at Mailchimp → Account → Extras → API keys."
    },
    {
      "id": "MAILCHIMP_BASE_URL",
      "envVar": "MAILCHIMP_BASE_URL",
      "label": "Override the API base URL (for mock servers or testing)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the API base URL (for mock servers or testing). Defaults to 'https://{dc}.api.mailchimp.com/3.0' derived from the API key suffix."
    },
    {
      "id": "MAILCHIMP_TIMEOUT_MS",
      "envVar": "MAILCHIMP_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds.",
      "default": "60000"
    },
    {
      "id": "MAILCHIMP_MAX_RETRIES",
      "envVar": "MAILCHIMP_MAX_RETRIES",
      "label": "Max retry attempts for transient upstream failures (0-10)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Max retry attempts for transient upstream failures (0-10).",
      "default": "3"
    },
    {
      "id": "MAILCHIMP_CONCURRENCY_LIMIT",
      "envVar": "MAILCHIMP_CONCURRENCY_LIMIT",
      "label": "Max in-flight upstream requests per workflow tool (1-10)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Max in-flight upstream requests per workflow tool (1-10).",
      "default": "4"
    },
    {
      "id": "MAILCHIMP_ASSETS_DIR",
      "envVar": "MAILCHIMP_ASSETS_DIR",
      "label": "Absolute path to a local assets directory",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Absolute path to a local assets directory. When set (Node-only — Workers ignored), enables the `mailchimp_assets` tool and auto-uploads `@assets/<path>` references in campaign HTML to Mailchimp File Manager at send time. Cache at <dir>/.mailchimp-cache.json."
    },
    {
      "id": "MAILCHIMP_TEMPLATES_DIR",
      "envVar": "MAILCHIMP_TEMPLATES_DIR",
      "label": "Absolute path to a local templates directory",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Absolute path to a local templates directory. When set (Node-only), enables the `mailchimp_local_templates` tool and support for `content.localTemplate` on campaign tools. Templates are `.eta` files with optional `<name>.meta.yaml` sidecars. Canonical write path on free-tier Mailchimp."
    },
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Sets the minimum log level for output (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Sets the minimum log level for output (e.g., 'debug', 'info', 'warn').",
      "default": "info"
    }
  ]
}
