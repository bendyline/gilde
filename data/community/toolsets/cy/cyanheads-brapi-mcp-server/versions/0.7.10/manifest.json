{
  "schemaVersion": 1,
  "version": "0.7.10",
  "releasedAt": "2026-07-16T20:41:56.757514Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@cyanheads/brapi-mcp-server",
    "version": "0.7.10",
    "sha256": "653bf6e4cfffc56b19d8bd1b626efe435727e3e8a0006f726d44420d6501bdd8",
    "entry": "dist/index.js",
    "args": [
      "run",
      "start:stdio"
    ],
    "envHints": [
      "BRAPI_DEFAULT_BASE_URL",
      "BRAPI_ENABLE_WRITES",
      "BRAPI_PAGE_SIZE",
      "MCP_LOG_LEVEL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "BRAPI_DEFAULT_BASE_URL",
      "envVar": "BRAPI_DEFAULT_BASE_URL",
      "label": "Default BrAPI v2 base URL",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Default BrAPI v2 base URL. Optional — connections can be opened at runtime via the brapi_connect tool instead."
    },
    {
      "id": "BRAPI_ENABLE_WRITES",
      "envVar": "BRAPI_ENABLE_WRITES",
      "label": "Opt-in flag for the write surface (brapi_submit_observations)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Opt-in flag for the write surface (brapi_submit_observations). Off by default — the tool is omitted from tools/list unless the operator opts in.",
      "default": "false"
    },
    {
      "id": "BRAPI_PAGE_SIZE",
      "envVar": "BRAPI_PAGE_SIZE",
      "label": "Upstream pageSize used during canvas spillover walks (decoupled from BRAPI_LOAD_LIMIT)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Upstream pageSize used during canvas spillover walks (decoupled from BRAPI_LOAD_LIMIT). Dataframe ceiling = pageSize × 50 pages.",
      "default": "1000"
    },
    {
      "id": "MCP_LOG_LEVEL",
      "envVar": "MCP_LOG_LEVEL",
      "label": "Sets the minimum log level for output (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Sets the minimum log level for output (e.g., 'debug', 'info', 'warn').",
      "default": "info"
    }
  ]
}
