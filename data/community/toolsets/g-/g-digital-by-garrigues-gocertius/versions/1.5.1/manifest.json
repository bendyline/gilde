{
  "schemaVersion": 1,
  "version": "1.5.1",
  "releasedAt": "2026-07-22T13:51:18.257047Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@g-digital/mcp-gocertius",
    "version": "1.5.1",
    "sha256": "b1d8f9e38ad1d0906fe18fc254713635b7721f79e136f3fed71520eb5597d934",
    "entry": "dist/server.js",
    "args": [],
    "envHints": [
      "MCP_ALLOW_INSECURE_FILE_URL",
      "MCP_ALLOW_UNVERIFIED_BEARER",
      "MCP_ALLOWED_HOSTS",
      "MCP_ALLOWED_ORIGINS",
      "MCP_API_BASE_URL",
      "MCP_AUTH_EMAIL",
      "MCP_AUTH_PASSWORD",
      "MCP_AUTH_USER_KEY",
      "MCP_HTTP_HOST",
      "MCP_HTTP_MAX_BODY_BYTES",
      "MCP_HTTP_PUBLIC",
      "MCP_SVC_CLIENT_ID",
      "MCP_SVC_CLIENT_SECRET",
      "MCP_SVC_INTROSPECT_URL",
      "PORT"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MCP_ALLOW_INSECURE_FILE_URL",
      "envVar": "MCP_ALLOW_INSECURE_FILE_URL",
      "label": "Set to \"true\" to allow plain http:// fileUrl downloads in evidence_create (default https-only)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" to allow plain http:// fileUrl downloads in evidence_create (default https-only). Private/internal addresses are rejected regardless (resolution-time check; see the documented DNS TOCTOU limitation in the hosted-deployment runbook)."
    },
    {
      "id": "MCP_ALLOW_UNVERIFIED_BEARER",
      "envVar": "MCP_ALLOW_UNVERIFIED_BEARER",
      "label": "Escape hatch for MCP_HTTP_PUBLIC=true WITHOUT inbound-token introspection: set to \"true\" ONLY when an upstream gateway already verifies Bearer tokens",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Escape hatch for MCP_HTTP_PUBLIC=true WITHOUT inbound-token introspection: set to \"true\" ONLY when an upstream gateway already verifies Bearer tokens. The server logs a prominent warning and forwards tokens upstream unverified."
    },
    {
      "id": "MCP_ALLOWED_HOSTS",
      "envVar": "MCP_ALLOWED_HOSTS",
      "label": "Comma-separated allowed Host headers",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated allowed Host headers. Empty = Host validation disabled (default). When set, requests with a Host outside the list are rejected."
    },
    {
      "id": "MCP_ALLOWED_ORIGINS",
      "envVar": "MCP_ALLOWED_ORIGINS",
      "label": "Comma-separated allowed browser Origins (DNS-rebinding defense)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated allowed browser Origins (DNS-rebinding defense). Empty = reject any request carrying an Origin header; non-browser clients (CLI/SDK) send no Origin and are always allowed. Use '*' to allow all."
    },
    {
      "id": "MCP_API_BASE_URL",
      "envVar": "MCP_API_BASE_URL",
      "label": "Overrides the default upstream API host for every tool",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Overrides the default upstream API host for every tool"
    },
    {
      "id": "MCP_AUTH_EMAIL",
      "envVar": "MCP_AUTH_EMAIL",
      "label": "Your GoCertius account email",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Your GoCertius account email. Configure exactly one flow; do not combine it with the others."
    },
    {
      "id": "MCP_AUTH_PASSWORD",
      "envVar": "MCP_AUTH_PASSWORD",
      "label": "Your GoCertius account password",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Your GoCertius account password. (See https://www.gocertius.io for credential acquisition.)"
    },
    {
      "id": "MCP_AUTH_USER_KEY",
      "envVar": "MCP_AUTH_USER_KEY",
      "label": "Long-lived GoCertius user key, exchanged automatically for a short-lived session token",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Long-lived GoCertius user key, exchanged automatically for a short-lived session token. Use it for headless or automated access instead of an account password. Configure exactly one flow; do not combine it with the others. (See https://www.gocertius.io for credential acquisition.)"
    },
    {
      "id": "MCP_HTTP_HOST",
      "envVar": "MCP_HTTP_HOST",
      "label": "Interface the HTTP transport binds to",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Interface the HTTP transport binds to. Default 127.0.0.1 (localhost only). Set 0.0.0.0 to expose on all interfaces (containers do this automatically)."
    },
    {
      "id": "MCP_HTTP_MAX_BODY_BYTES",
      "envVar": "MCP_HTTP_MAX_BODY_BYTES",
      "label": "Maximum accepted POST /mcp request-body size in bytes (default 16777216 = 16 MiB — sized so base64 file uploads within the documented tool limits fit)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum accepted POST /mcp request-body size in bytes (default 16777216 = 16 MiB — sized so base64 file uploads within the documented tool limits fit). Oversized requests get a 413 JSON-RPC error before/while reading — closes a memory-exhaustion DoS vector in public deployments. Note: base64 file sources are capped by this limit BEFORE MCP_FILE_MAX_BYTES applies."
    },
    {
      "id": "MCP_HTTP_PUBLIC",
      "envVar": "MCP_HTTP_PUBLIC",
      "label": "Set to \"true\" for public/multi-tenant deployments",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to \"true\" for public/multi-tenant deployments. Activates Host validation and refuses to start unless (1) MCP_ALLOWED_ORIGINS or MCP_ALLOWED_HOSTS is set AND (2) inbound Bearer introspection is configured (MCP_SVC_INTROSPECT_URL + MCP_SVC_CLIENT_ID/SECRET) or MCP_ALLOW_UNVERIFIED_BEARER=true is set explicitly (fail-closed)."
    },
    {
      "id": "MCP_SVC_CLIENT_ID",
      "envVar": "MCP_SVC_CLIENT_ID",
      "label": "Client ID this server presents to the introspection endpoint above (its resource-server credentials)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Client ID this server presents to the introspection endpoint above (its resource-server credentials). Only needed alongside MCP_SVC_INTROSPECT_URL; it does not authenticate you to GoCertius."
    },
    {
      "id": "MCP_SVC_CLIENT_SECRET",
      "envVar": "MCP_SVC_CLIENT_SECRET",
      "label": "Client secret this server presents to the introspection endpoint above (its resource-server credentials)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Client secret this server presents to the introspection endpoint above (its resource-server credentials). Only needed alongside MCP_SVC_INTROSPECT_URL; it does not authenticate you to GoCertius. (See https://www.gocertius.io for credential acquisition.)"
    },
    {
      "id": "MCP_SVC_INTROSPECT_URL",
      "envVar": "MCP_SVC_INTROSPECT_URL",
      "label": "RFC 7662 token introspection URL for inbound Bearer verification in HTTP mode",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "RFC 7662 token introspection URL for inbound Bearer verification in HTTP mode. Opt-in, and required when MCP_HTTP_PUBLIC=true. Leave empty for stdio (local) use."
    },
    {
      "id": "PORT",
      "envVar": "PORT",
      "label": "HTTP port when running in hosted (HTTP) mode; ignored in stdio mode",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "HTTP port when running in hosted (HTTP) mode; ignored in stdio mode"
    }
  ]
}
