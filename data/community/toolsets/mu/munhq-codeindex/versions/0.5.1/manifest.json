{
  "schemaVersion": 1,
  "version": "0.5.1",
  "releasedAt": "2026-09-13T10:39:02.451533Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@munhq/codeindex",
    "version": "0.5.1",
    "sha256": "7bc4e718f5520dc20281c95b522c6fca86cec5c4a192dce45263bab0d6ce5823",
    "entry": "bin/codeindex-mcp.js",
    "args": [],
    "envHints": [
      "CODEINDEX_WORKSPACE",
      "CODEINDEX_NO_DAEMON",
      "CODEINDEX_DAEMON_IDLE_SECS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "CODEINDEX_WORKSPACE",
      "envVar": "CODEINDEX_WORKSPACE",
      "label": "Directory to index",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Directory to index. Defaults to the enclosing project root of wherever the client starts the server, which is the right answer for every editor and agent that launches it inside a repository. Set it only when the server runs somewhere other than the code."
    },
    {
      "id": "CODEINDEX_NO_DAEMON",
      "envVar": "CODEINDEX_NO_DAEMON",
      "label": "Set to 1 to keep the index inside this server process",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to 1 to keep the index inside this server process. By default every session on one repository shares a single background daemon that parses and watches the tree once, and each session is a thin relay onto it. Turn that off for a sandbox with no writable runtime directory."
    },
    {
      "id": "CODEINDEX_DAEMON_IDLE_SECS",
      "envVar": "CODEINDEX_DAEMON_IDLE_SECS",
      "label": "Seconds with no session attached before the shared daemon exits and returns its memory",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Seconds with no session attached before the shared daemon exits and returns its memory. Default 900. Set 0 to keep it resident."
    }
  ]
}
