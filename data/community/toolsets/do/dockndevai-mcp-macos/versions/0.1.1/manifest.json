{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-09-12T17:57:56.397487Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@dockndevai/mcp-macos",
    "version": "0.1.1",
    "sha256": "016609bde003868134e258d931a783a77d9fb993a8acebf5b169dad3b4fe86a4",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "MACOS_MODE",
      "MACOS_ALLOW_EXEC",
      "MACOS_ALLOW_DELETE",
      "MACOS_ALLOW_INPUT",
      "MACOS_PATH_ALLOWLIST"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "MACOS_MODE",
      "envVar": "MACOS_MODE",
      "label": "Access mode: read-only | read-write | admin",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Access mode: read-only | read-write | admin. Starts read-only; writes need read-write."
    },
    {
      "id": "MACOS_ALLOW_EXEC",
      "envVar": "MACOS_ALLOW_EXEC",
      "label": "Set true to allow run_command / run_applescript / kill_process (admin mode)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set true to allow run_command / run_applescript / kill_process (admin mode)."
    },
    {
      "id": "MACOS_ALLOW_DELETE",
      "envVar": "MACOS_ALLOW_DELETE",
      "label": "Set true to allow delete_path (moves to Trash; admin mode)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set true to allow delete_path (moves to Trash; admin mode)."
    },
    {
      "id": "MACOS_ALLOW_INPUT",
      "envVar": "MACOS_ALLOW_INPUT",
      "label": "Set true to allow GUI input (type/key/click; admin mode)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set true to allow GUI input (type/key/click; admin mode). Needs Accessibility permission."
    },
    {
      "id": "MACOS_PATH_ALLOWLIST",
      "envVar": "MACOS_PATH_ALLOWLIST",
      "label": "Comma-separated path roots the agent may touch (empty = anywhere; protected paths still apply)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated path roots the agent may touch (empty = anywhere; protected paths still apply)."
    }
  ]
}
