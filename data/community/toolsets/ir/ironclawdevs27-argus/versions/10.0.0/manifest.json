{
  "schemaVersion": 1,
  "version": "10.0.0",
  "releasedAt": "2026-07-18T17:26:27.18231Z",
  "runtime": {
    "kind": "npm-package",
    "package": "argusqa-os",
    "version": "10.0.0",
    "sha256": "7ffc3bd879dc752a45724c16e475d4f1e4ada59435bce775aa6a07c81feb2249",
    "entry": "src/cli/init.js",
    "args": [],
    "envHints": [
      "TARGET_DEV_URL",
      "TARGET_STAGING_URL",
      "SLACK_BOT_TOKEN",
      "FIGMA_API_TOKEN",
      "ARGUS_REDACT_SENSITIVE"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "TARGET_DEV_URL",
      "envVar": "TARGET_DEV_URL",
      "label": "Base URL of the dev environment to audit (e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Base URL of the dev environment to audit (e.g. http://localhost:3000)"
    },
    {
      "id": "TARGET_STAGING_URL",
      "envVar": "TARGET_STAGING_URL",
      "label": "Base URL of the staging environment — used by argus_compare for dev vs staging diff",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Base URL of the staging environment — used by argus_compare for dev vs staging diff. Omit to use CSS analysis mode."
    },
    {
      "id": "SLACK_BOT_TOKEN",
      "envVar": "SLACK_BOT_TOKEN",
      "label": "Slack bot token (xoxb-",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Slack bot token (xoxb-...) for posting reports. Omit to generate a local report.html instead."
    },
    {
      "id": "FIGMA_API_TOKEN",
      "envVar": "FIGMA_API_TOKEN",
      "label": "Figma personal access token — enables argus_design_audit to fetch design specs from a Figma frame and compare them against the live DOM (13 fidelity finding types)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Figma personal access token — enables argus_design_audit to fetch design specs from a Figma frame and compare them against the live DOM (13 fidelity finding types). Omit to skip design fidelity audits."
    },
    {
      "id": "ARGUS_REDACT_SENSITIVE",
      "envVar": "ARGUS_REDACT_SENSITIVE",
      "label": "Aegis confidentiality egress boundary",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Aegis confidentiality egress boundary. Default ON: every tool response is redacted so secrets, PII, and exploit detail never cross into the calling agent's context (the local on-disk report keeps full fidelity). Set to 0 for byte-identical pre-Aegis output."
    }
  ]
}
