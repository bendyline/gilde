{
  "schemaVersion": 1,
  "version": "2.7.0",
  "releasedAt": "2026-08-22T02:03:36.312257Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@ckalima/pipedrive-mcp-server",
    "version": "2.7.0",
    "sha256": "f9c36d345382fe0ddea4e3d32f4287d502edcf4b296eaf19591c8fbdc901e455",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "PIPEDRIVE_API_KEY",
      "PIPEDRIVE_MODE",
      "PIPEDRIVE_ENABLE_DESTRUCTIVE",
      "PIPEDRIVE_IMAGE_BASE_DIR"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "PIPEDRIVE_API_KEY",
      "envVar": "PIPEDRIVE_API_KEY",
      "label": "Your 40-character Pipedrive API token (Settings > Personal preferences > API)",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Your 40-character Pipedrive API token (Settings > Personal preferences > API)."
    },
    {
      "id": "PIPEDRIVE_MODE",
      "envVar": "PIPEDRIVE_MODE",
      "label": "Server-enforced capability tier: 'read-only' (reads only), 'safe-write' (reads + non-destructive writes; the default), or 'full' (all tools, including destructive)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Server-enforced capability tier: 'read-only' (reads only), 'safe-write' (reads + non-destructive writes; the default), or 'full' (all tools, including destructive). Out-of-mode tools are hidden from tools/list and refused if called directly. A blank value is treated as unset (defaults to safe-write); an unrecognized value falls back to read-only."
    },
    {
      "id": "PIPEDRIVE_ENABLE_DESTRUCTIVE",
      "envVar": "PIPEDRIVE_ENABLE_DESTRUCTIVE",
      "label": "Legacy flag, superseded by PIPEDRIVE_MODE",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Legacy flag, superseded by PIPEDRIVE_MODE. When PIPEDRIVE_MODE is unset, 'true' maps to full; otherwise prefer PIPEDRIVE_MODE=full. Still honored for back-compat."
    },
    {
      "id": "PIPEDRIVE_IMAGE_BASE_DIR",
      "envVar": "PIPEDRIVE_IMAGE_BASE_DIR",
      "label": "Allowlisted directory the server may read product images from when a file_path is passed to the image-upload tools",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Allowlisted directory the server may read product images from when a file_path is passed to the image-upload tools. Filesystem reads are disabled unless this is set, and a file_path must resolve within it."
    }
  ]
}
