{
  "schemaVersion": 1,
  "version": "2.0.1",
  "releasedAt": "2026-08-24T20:36:22.290843Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@hypnosis/docker-mcp-server",
    "version": "2.0.1",
    "sha256": "cf364b94487f0258cf2295c0a9e80ebdf615d7c97aaf0c14a7abb3c7660861a9",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "DOCKER_MCP_PROFILES_FILE",
      "DOCKER_PROFILES",
      "DOCKER_MCP_ALLOW_SSH_FALLBACK",
      "LOG_LEVEL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "DOCKER_MCP_PROFILES_FILE",
      "envVar": "DOCKER_MCP_PROFILES_FILE",
      "label": "Absolute path to the JSON file describing the Docker hosts this agent may reach",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Absolute path to the JSON file describing the Docker hosts this agent may reach. Without it the server answers about the Docker on this machine."
    },
    {
      "id": "DOCKER_PROFILES",
      "envVar": "DOCKER_PROFILES",
      "label": "The same profiles JSON inline, used when no profiles file is set",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "The same profiles JSON inline, used when no profiles file is set."
    },
    {
      "id": "DOCKER_MCP_ALLOW_SSH_FALLBACK",
      "envVar": "DOCKER_MCP_ALLOW_SSH_FALLBACK",
      "label": "Keep going when a profile's SSH key is missing, instead of refusing the profile",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Keep going when a profile's SSH key is missing, instead of refusing the profile. Defaults to false."
    },
    {
      "id": "LOG_LEVEL",
      "envVar": "LOG_LEVEL",
      "label": "Log verbosity: error, warn, info or debug",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Log verbosity: error, warn, info or debug. Defaults to info."
    }
  ]
}
