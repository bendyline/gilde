{
  "schemaVersion": 1,
  "version": "4.2.0",
  "releasedAt": "2026-09-05T10:48:47.259599Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@smartergpt/lex-mcp",
    "version": "4.2.0",
    "sha256": "38a14469641180e772c4c25d27800069b024f16debdb27495246f911dafc50fd",
    "entry": "index.mjs",
    "args": [],
    "envHints": [
      "LEX_WORKSPACE_ROOT",
      "LEX_STORE",
      "LEX_DB_PATH",
      "LEX_MEMORY_DB",
      "LEX_DATABASE_URL",
      "LEX_POSTGRES_PASSWORD",
      "LEX_POSTGRES_POOL_MAX",
      "LEX_DEBUG"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "LEX_WORKSPACE_ROOT",
      "envVar": "LEX_WORKSPACE_ROOT",
      "label": "Project root directory for Lex runtime discovery",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Project root directory for Lex runtime discovery. Defaults to current working directory."
    },
    {
      "id": "LEX_STORE",
      "envVar": "LEX_STORE",
      "label": "Frame-store backend: sqlite or postgres",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Frame-store backend: sqlite or postgres. Defaults to sqlite unless LEX_DATABASE_URL selects PostgreSQL."
    },
    {
      "id": "LEX_DB_PATH",
      "envVar": "LEX_DB_PATH",
      "label": "Path to SQLite database file",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Path to SQLite database file. Defaults to .smartergpt/lex/memory.db under the project root."
    },
    {
      "id": "LEX_MEMORY_DB",
      "envVar": "LEX_MEMORY_DB",
      "label": "Alias for LEX_DB_PATH (backwards compatibility)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Alias for LEX_DB_PATH (backwards compatibility). Same behavior."
    },
    {
      "id": "LEX_DATABASE_URL",
      "envVar": "LEX_DATABASE_URL",
      "label": "PostgreSQL connection URL for the compatibility adapter",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "PostgreSQL connection URL for the compatibility adapter. Treat as secret because it may contain credentials."
    },
    {
      "id": "LEX_POSTGRES_PASSWORD",
      "envVar": "LEX_POSTGRES_PASSWORD",
      "label": "PostgreSQL password supplied separately when LEX_DATABASE_URL omits one",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "PostgreSQL password supplied separately when LEX_DATABASE_URL omits one."
    },
    {
      "id": "LEX_POSTGRES_POOL_MAX",
      "envVar": "LEX_POSTGRES_POOL_MAX",
      "label": "Maximum PostgreSQL compatibility-adapter pool size",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Maximum PostgreSQL compatibility-adapter pool size. Defaults to 10."
    },
    {
      "id": "LEX_DEBUG",
      "envVar": "LEX_DEBUG",
      "label": "Set to any value to enable debug logging to stderr",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Set to any value to enable debug logging to stderr."
    }
  ]
}
