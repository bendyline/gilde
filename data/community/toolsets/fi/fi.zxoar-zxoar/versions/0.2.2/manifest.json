{
  "schemaVersion": 1,
  "version": "0.2.2",
  "releasedAt": "2026-07-22T19:17:40.405353Z",
  "runtime": {
    "kind": "npm-package",
    "package": "zxoar-mcp",
    "version": "0.2.2",
    "sha256": "53141504004863a4adf6dc9cffce7fb6b9e5b878a8a3ae6ae439bbed04b481e5",
    "entry": "src/index.mjs",
    "args": [],
    "envHints": [
      "PRIVATE_KEY",
      "MAX_ETH_PER_TX",
      "MAX_ETH_PER_SESSION",
      "MAX_TX_PER_MINUTE",
      "RPC_URL"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "PRIVATE_KEY",
      "envVar": "PRIVATE_KEY",
      "label": "Optional trading key — without it the server is READ-ONLY (scan/vet/info only)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional trading key — without it the server is READ-ONLY (scan/vet/info only). Signs locally; never leaves your machine."
    },
    {
      "id": "MAX_ETH_PER_TX",
      "envVar": "MAX_ETH_PER_TX",
      "label": "Server-enforced cap per trade in ETH (default 0",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Server-enforced cap per trade in ETH (default 0.5)."
    },
    {
      "id": "MAX_ETH_PER_SESSION",
      "envVar": "MAX_ETH_PER_SESSION",
      "label": "Server-enforced cumulative session spend cap in ETH (default 2)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Server-enforced cumulative session spend cap in ETH (default 2)."
    },
    {
      "id": "MAX_TX_PER_MINUTE",
      "envVar": "MAX_TX_PER_MINUTE",
      "label": "Server-enforced rate limit on state-changing calls (default 12)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Server-enforced rate limit on state-changing calls (default 12)."
    },
    {
      "id": "RPC_URL",
      "envVar": "RPC_URL",
      "label": "Override the chain RPC (defaults to Robinhood Chain mainnet; pair with CHAIN_ID and the *_ADDRESS vars for forks/testnets)",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Override the chain RPC (defaults to Robinhood Chain mainnet; pair with CHAIN_ID and the *_ADDRESS vars for forks/testnets)."
    }
  ]
}
