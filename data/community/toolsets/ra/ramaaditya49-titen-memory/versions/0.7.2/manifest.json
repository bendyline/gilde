{
  "schemaVersion": 1,
  "version": "0.7.2",
  "releasedAt": "2026-08-08T05:28:03.706882Z",
  "runtime": {
    "kind": "npm-package",
    "package": "titen-memory",
    "version": "0.7.2",
    "sha256": "1e20cdfcff65e99ec063da8c8dfaeab8caa26ac92392a1701a11688af1b25a5c",
    "entry": "src/runtime/bun/cli.ts",
    "args": [
      "mcp"
    ],
    "envHints": [
      "TITEN_MCP_URL",
      "TITEN_API_KEY"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "TITEN_MCP_URL",
      "envVar": "TITEN_MCP_URL",
      "label": "Optional",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional. Streamable HTTP /mcp endpoint of a running Titen service. Set together with TITEN_API_KEY to use a shared store instead of the local one."
    },
    {
      "id": "TITEN_API_KEY",
      "envVar": "TITEN_API_KEY",
      "label": "Optional",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Optional. API key for the Titen service named by TITEN_MCP_URL. Required only when TITEN_MCP_URL is set."
    }
  ]
}
