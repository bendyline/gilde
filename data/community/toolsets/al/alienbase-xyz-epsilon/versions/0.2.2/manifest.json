{
  "schemaVersion": 1,
  "version": "0.2.2",
  "releasedAt": "2026-08-28T04:02:46.551717Z",
  "runtime": {
    "kind": "npm-package",
    "package": "@epsilon-exchange/mcp",
    "version": "0.2.2",
    "sha256": "7a6411a9c213c3209e3523b8b9888bd2f613537baf459223a9a49b2e7cccf5e4",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "EPSILON_API_KEY",
      "EPSILON_WALLET_KEY",
      "EPSILON_REFERRER"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "EPSILON_API_KEY",
      "envVar": "EPSILON_API_KEY",
      "label": "Epsilon developer key (eps_…) — create one at developers",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Epsilon developer key (eps_…) — create one at developers.epsilon.exchange"
    },
    {
      "id": "EPSILON_WALLET_KEY",
      "envVar": "EPSILON_WALLET_KEY",
      "label": "0x-prefixed private key of a dedicated trading wallet; omit for read-only (quotes, order status, market stats)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "0x-prefixed private key of a dedicated trading wallet; omit for read-only (quotes, order status, market stats)"
    },
    {
      "id": "EPSILON_REFERRER",
      "envVar": "EPSILON_REFERRER",
      "label": "Optional rev-share payout address (0x…): the referral leg of every fill routed by this server is paid there on-chain",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Optional rev-share payout address (0x…): the referral leg of every fill routed by this server is paid there on-chain. Never changes the maker's fee — it only redirects the existing leg."
    },
    {
      "id": "X-API-Key",
      "envVar": "X-API-Key",
      "label": "Epsilon developer key (eps_…) — create one at developers",
      "type": "string",
      "secret": true,
      "required": true,
      "multiline": false,
      "description": "Epsilon developer key (eps_…) — create one at developers.epsilon.exchange"
    }
  ]
}
