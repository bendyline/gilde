{
  "schemaVersion": 1,
  "version": "1.1.0",
  "releasedAt": "2026-08-23T03:13:09.545625Z",
  "runtime": {
    "kind": "npm-package",
    "package": "mcp-shopify-admin",
    "version": "1.1.0",
    "sha256": "d947e6541b3265c5851b315f5d67582926fd0eaeb6c5215d00c17365e11f7a45",
    "entry": "dist/index.js",
    "args": [],
    "envHints": [
      "SHOPIFY_STORE_DOMAIN",
      "SHOPIFY_ACCESS_TOKEN",
      "SHOPIFY_CLIENT_ID",
      "SHOPIFY_CLIENT_SECRET",
      "SHOPIFY_API_VERSION",
      "SHOPIFY_API_BASE",
      "SHOPIFY_TIMEOUT_MS",
      "SHOPIFY_MAX_RETRIES",
      "SHOPIFY_TOKEN_LEEWAY_SECONDS"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "SHOPIFY_STORE_DOMAIN",
      "envVar": "SHOPIFY_STORE_DOMAIN",
      "label": "Permanent store host, e",
      "type": "string",
      "secret": false,
      "required": true,
      "multiline": false,
      "description": "Permanent store host, e.g. my-store.myshopify.com (a bare store name works too)."
    },
    {
      "id": "SHOPIFY_ACCESS_TOKEN",
      "envVar": "SHOPIFY_ACCESS_TOKEN",
      "label": "Ready-made Admin API access token (legacy apps created before 2026-01-01)",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Ready-made Admin API access token (legacy apps created before 2026-01-01). Alternative to the client id/secret pair."
    },
    {
      "id": "SHOPIFY_CLIENT_ID",
      "envVar": "SHOPIFY_CLIENT_ID",
      "label": "Client id of a Dev Dashboard app; with the secret the server mints and refreshes its own token",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Client id of a Dev Dashboard app; with the secret the server mints and refreshes its own token."
    },
    {
      "id": "SHOPIFY_CLIENT_SECRET",
      "envVar": "SHOPIFY_CLIENT_SECRET",
      "label": "Client secret of that app",
      "type": "string",
      "secret": true,
      "required": false,
      "multiline": false,
      "description": "Client secret of that app. Treat it as a secret."
    },
    {
      "id": "SHOPIFY_API_VERSION",
      "envVar": "SHOPIFY_API_VERSION",
      "label": "Admin API version: a quarterly YYYY-MM release or unstable",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Admin API version: a quarterly YYYY-MM release or unstable.",
      "default": "2026-01"
    },
    {
      "id": "SHOPIFY_API_BASE",
      "envVar": "SHOPIFY_API_BASE",
      "label": "Full http/https URL of the GraphQL endpoint, replacing host and version (e",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Full http/https URL of the GraphQL endpoint, replacing host and version (e.g. a local mock)."
    },
    {
      "id": "SHOPIFY_TIMEOUT_MS",
      "envVar": "SHOPIFY_TIMEOUT_MS",
      "label": "Per-request timeout in milliseconds",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Per-request timeout in milliseconds.",
      "default": "30000"
    },
    {
      "id": "SHOPIFY_MAX_RETRIES",
      "envVar": "SHOPIFY_MAX_RETRIES",
      "label": "Retries on THROTTLED/429 (always) and on 5xx / network errors for reads only",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Retries on THROTTLED/429 (always) and on 5xx / network errors for reads only.",
      "default": "4"
    },
    {
      "id": "SHOPIFY_TOKEN_LEEWAY_SECONDS",
      "envVar": "SHOPIFY_TOKEN_LEEWAY_SECONDS",
      "label": "Re-mint the token this many seconds before it expires",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Re-mint the token this many seconds before it expires.",
      "default": "300"
    }
  ]
}
