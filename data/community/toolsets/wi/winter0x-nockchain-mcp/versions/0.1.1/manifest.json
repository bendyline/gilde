{
  "schemaVersion": 1,
  "version": "0.1.1",
  "releasedAt": "2026-08-25T11:08:10.464913Z",
  "runtime": {
    "kind": "npm-package",
    "package": "nockchain-mcp",
    "version": "0.1.1",
    "sha256": "5f75dc251d5255f13fe29c29202ec60a3dbde163e1ad6ce3828420743773c5cb",
    "entry": "dist/bin.js",
    "args": [],
    "envHints": [
      "NOCKCHAIN_ENDPOINTS",
      "NOCKCHAIN_MAX_STALENESS_SECONDS",
      "NOCKCHAIN_MAX_HEIGHT_LAG"
    ]
  },
  "tools": [],
  "config": [
    {
      "id": "NOCKCHAIN_ENDPOINTS",
      "envVar": "NOCKCHAIN_ENDPOINTS",
      "label": "Comma-separated host:port list that replaces the default endpoint pool",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Comma-separated host:port list that replaces the default endpoint pool. Prefix an entry with tls:// or plain:// to override the scheme."
    },
    {
      "id": "NOCKCHAIN_MAX_STALENESS_SECONDS",
      "envVar": "NOCKCHAIN_MAX_STALENESS_SECONDS",
      "label": "Reject an endpoint whose chain state is older than this",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Reject an endpoint whose chain state is older than this. Defaults to 900."
    },
    {
      "id": "NOCKCHAIN_MAX_HEIGHT_LAG",
      "envVar": "NOCKCHAIN_MAX_HEIGHT_LAG",
      "label": "Reject an endpoint trailing the best height in the pool by more than this",
      "type": "string",
      "secret": false,
      "required": false,
      "multiline": false,
      "description": "Reject an endpoint trailing the best height in the pool by more than this. Defaults to 3."
    }
  ]
}
